# UX-11 U3 — Fresh §47 Gate Battery + 13-Item Freeze Mapping

> Date: 2026-09-18 · Tree: starting HEAD `3f70887477ba96ef7f232155e0684679591d6b48` (after U1 `130ae2b`, U2 `cbb72e3` + `af30be2`)
> All results from THIS session's runs — never copied from UX-10.

## Gate battery (raw results, run on 2026-09-18)

| Gate | Command | Exit | Result |
|---|---|---|---|
| tsc | `pnpm exec tsc --noEmit` | 0 | 0 errors |
| lint | `pnpm lint` | 0 | 0 errors, 7 warnings (all pre-existing `src/**` files: `nav.tsx`, `email-flows.test.ts`, `auth-atomicity.test.ts`, `concurrency-write-skew-pairs.test.ts`) |
| parity | `pnpm parity` | 0 | 2 files / 4 tests passed (6.31 s) |
| build | `pnpm build` | 0 | "Compiled successfully in 11.6 s"; production routes emitted (includes POST /transfers... ƒ Proxy) |
| test | `pnpm test` (timeout 2,900,000 ms — regulatory) | 0 | **Test Files 151 passed (151) · Tests 1568 passed (1568)** · Duration 1463.77 s (~24.4 min) |

## §47 freeze mapping — EXACTLY 13 items

Source: `docs/freeze + UX-UI.md` §47 lines 1487–1499 (13 bullets). All items pass in this fresh run (suite 1568/1568 above).

| # | §47 item | Concrete test evidence (all PASS in this run) |
|---|---|---|
| 1 | crear movimiento (create) | `core/application/movements/movements.test.ts` → "creates a manual income movement" (L142), "creates a manual expense movement" (L178) + 34 its in file; `infrastructure/repositories/movement-repository.test.ts` |
| 2 | editar movimiento (edit) | `movements.test.ts` → "touches the current account on amount-only edits" (R15.3 §7, L722), "does not touch account or category on cosmetic-only edits" (L834); `concurrency-movements.test.ts` |
| 3 | eliminar movimiento (delete) | `movements.test.ts` → "deletes a manual movement and touches its account (R15.3.2)" (L953); `infrastructure/transactions/concurrency-deletion.test.ts` |
| 4 | transferencias | `core/application/transfers/transfers.test.ts` → createTransfer block: "creates a same-currency transfer (TRA-2)" (L252), "throws ValidationError when source = destination (TRA-1)" (L565); `infrastructure/mappers/transfer.test.ts`, `transfer-repository.test.ts` |
| 5 | multimoneda | `transfers.test.ts` → "derives effectiveExchangeRate from both amounts for cross-currency (TRA-3)" (L515); `account-repository.test.ts`, `movement-repository.test.ts` (currency fields); e2e `e2e/multicurrency.spec.ts` (suite not re-run — see note) |
| 6 | saldo negativo (negative balance) | `core/application/balance.test.ts` → "handles negative balances correctly" (L202); e2e `e2e/negative-balance.spec.ts` (suite not re-run — see note) |
| 7 | créditos (received & granted) | `core/application/credits-received/credits-received.test.ts`, `credits-granted/credits-granted.test.ts` → "creates credit with optional installments and frequency" (L343); e2e `e2e/credits-pos.spec.ts` |
| 8 | abonos (installments/payments) | `credits-granted.test.ts` → "adds an abono and creates income movement (debtor pays back)" (L435), "sets context to Personal (hardcoded) for credit granted abono movement" (L468); `infrastructure/transactions/concurrency-abonos.test.ts` |
| 9 | POS (catalog/sales) | `core/application/sales/sales.test.ts`, `core/application/sales/get-sale-detail.test.ts`; e2e `e2e/credits-pos.spec.ts` POS blocks; `payables.test.ts` (buy-on-credit obligations) |
| 10 | opening balances | `core/application/accounts/accounts.test.ts` (opening movement invariants); `infrastructure/repositories/movement-repository.test.ts` (`link.kind: opening` partial-unique backstop); e2e `e2e/duplicate-opening.spec.ts` |
| 11 | operaciones multi-cuenta | `transfers.test.ts` (source/destination account resolution + NotFoundError paths L452/L484); `account-repository.test.ts`; `concurrency-matrix-pairs.test.ts` |
| 12 | idempotencia | `infrastructure/models/idempotency.test.ts`, `infrastructure/auth/idempotency.test.ts`, `infrastructure/auth/idempotency-restart.test.ts`, `operation-log-repository.test.ts` |
| 13 | aislamiento tenant | `core/application/tenant-isolation.test.ts` → "updateAccount with user-b accountId → NotFoundError" (L398, L406, L416); `infrastructure/repositories/tenant-touch.test.ts`; `infrastructure/models/indexes.test.ts` (workspaceId partial-unique); `cas-restart.test.ts` |

**Note (UX-10 "15 rows" slip corrected):** the UX-10 verify table listed 15 rows for the §47 mapping; §47 of the freeze defines exactly 13 items (lines 1487–1499 count verified 2026-09-18). This table contains exactly 13 rows, one per item — the inherited off-by-two is hereby corrected.

**E2E suite note (not a §47 gate):** the five sanctioned gates are tsc/lint/parity/build/vitest; the Playwright e2e suite (`pnpm test:e2e`) is not part of the gate battery and was NOT re-run in this session — the item mapping above leans on the vitest evidence, which passed 100%. Any e2e reference in the table is marked "suite not re-run".

## Reproduction commands

```
pnpm exec tsc --noEmit          # exit 0
pnpm lint                       # exit 0 (0 errors)
pnpm parity                     # exit 0
pnpm build                      # exit 0
pnpm test                       # exit 0 — 151 files / 1568 tests passed, timeout 2900000 ms
```
