# TWINCAP — PLAN DE IMPLEMENTACIÓN

> **Este documento es el plan maestro y documento de continuidad del proyecto.**
>
> **Cualquier agente que trabaje en el proyecto DEBE leer `AGENTS.md` y este documento ANTES de empezar.**
>
> **Después de una compactación o en una nueva sesión, lo PRIMERO es leer este archivo.**
>
> Lineamientos de la ronda actual: sección `RONDA 14` de este archivo (plan vigente).

---

> ⚠️ **REGLA CRÍTICA (heredada de Ronda 2):** la ronda comienza con **AUDITORÍA**, no con implementación.
> Prohibido escribir código hasta presentar el plan definitivo por fases y recibir aprobación explícita del usuario.
> Cada fase se implementa SOLO con su aviso explícito (protocolo R3.13).

---

## ESTADO ACTUAL (última actualización: 2026-09-06)

| Ronda | Estado |
|-------|--------|
| Ronda 1 — fases 0–22 | ✅ Completa |
| Ronda 2 — Auditoría y mejoras | ✅ Completa (Fases 0–10 + post-ronda branding; detalle en git history del doc y `Ronda 2.md`) |
| Ronda 3 — Auditoría integral, corrección financiera, dashboard, evolución funcional | ✅ **COMPLETADA (2026-08-25).** 12 fases ejecutadas y verificadas. Auditoría final: 0 CRITICAL, 7 MINOR (no bloqueantes). Suite 412/412 tests en 42 archivos, tsc limpio. Fixes de usuario incluidos: sort por fecha, logout confirmation, placeholders "Seleccionar", transición theme suave, badge "Pagado" en créditos. |
| **Ronda 4 — Rediseño y evolución del Dashboard financiero** | ✅ **COMPLETADA (2026-08-26).** 6 fases (A→F). Dashboard rediseñado: jerarquía visual, tablas resumen ingresos/gastos por categoría, filtros composables, gráfico anual filtrado, movimientos responsive con columna Category. Suite 428/428, tsc, build limpios. Commits: `cd1bdd2`, `80dd92c`, `5dea992`, `10d6e0b`. |
| **Ronda 5 — Integridad financiera, créditos con cuotas, fix dashboard, onboarding** | ✅ **COMPLETADA (2026-08-28).** A ✅ `fd1a3d6` · B ✅ `154dac8` · C ✅ `0a68517` · D ✅ `e460b37` · E ✅ `a57c03b` (seed fija + onboarding) · **F ✅ `c5d198a` + `1396928`** (reparación legacy: 46 acciones aplicadas contra Atlas — 45 + relink post-QA; auditoría global con criterio de la app: 0 huérfanos, 0 ventas on-credit sin crédito, 0 transfers inválidas) · QA ✅ (suite 485/485, tsc, build). Commits previos de una ronda 5 anterior (no reemplazada): `c996ff1`, `3b073ea`, `b6dba3f`, `e523520`. |
| **Ronda 6 — Hallazgo post-R5: ventas eliminadas visibles** | ✅ **IMPLEMENTADA (2026-08-28).** Causa raíz estructural: lecturas de movimientos NO verificaban existencia del padre de `link.refId`; `deleteSale` no cubría movimientos con `refId` UUID legacy. **P1** filtro de lectura con reconciliación por valor (P1 v2), **P3** `deleteByRefId` (`deleteMany`), **P2** `deleteSale` cascada robusta por refId, **P4** tests, **P5** datos Atlas (1 relink de crédito vivo + 1 borrado de huérfano real). Suite **513/513**, tsc limpio. Detalle en sección R6. |
| **Ronda 7 — Raíz UUID↔ObjectId + cards infladas** | ✅ **COMPLETADA (2026-08-28).** R7-A dashboard deriva balances de movimientos filtrados P1; R7-B ids ObjectId persistidos como `_id`; R7-C 2 huérfanos de pruebas2 limpiados (idempotencia 0/0). Suite **517/517**, tsc, build. Detalle sección R7. |
| **Ronda 8 — Capital financiero fuera del resultado económico** | ✅ **COMPLETADA (2026-08-28).** `economic-result.ts` centraliza `NON_ECONOMIC_LINK_KINDS` (transfer, opening, creditReceivedPrincipal, creditGrantedPrincipal); card "Flujos de financiamiento" reemplaza "Patrimonio"; `financingInflow/Outflow`; currencyBreakdown filtrado. Suite **536/536**, tsc, build. Commits `f11c1b8`, `799aef8`, `dbd60e2`, `15f81cb`, `454f2fa`. |
| **Ronda 9 — Créditos otorgados: amortización capital/interés + baja incobrable · Fix cuentas con saldo inicial · Saldo inicial para cuenta existente** | ✅ **COMPLETADA (2026-08-30).** Regla del usuario: abonos de créditos otorgados amortizan primero el principal; solo el excedente es ingreso. Nueva acción "dar de baja crédito incobrable" registra gasto por capital no recuperado. **R9-H1:** cuentas creadas CON saldo inicial no se pueden eliminar (el movimiento `opening` propio bloquea el guard ACC-4). **R9-H2:** la cuenta fija "Efectivo" del seed no tiene opción para establecerle saldo inicial (solo `createAccount` al crear cuentas nuevas). Auditoría + plan en sección R9 (fases A–G). **Fase A COMPLETADA y DESPLEGADA (2026-08-29):** fix R9-H1 — commits `36998a1` + `64014dc`. **Fase B COMPLETADA (2026-08-29):** saldo inicial para cuenta existente (R9-H2) — commits `bc0d260` + `4fafa89`; suite **548/548**. **Fase C COMPLETADA y DESPLEGADA (2026-08-29):** dominio + kinds — suite **564/564**, tsc limpio; push `4757080`. **Fase D COMPLETADA (2026-08-29):** use cases granted (split en origen + edit/delete con 2 movimientos + write-off + action) — suite **586/586**, tsc limpio. **Fase E COMPLETADA (2026-08-29):** posición financiera (excluye writtenOff del activo en `computeActivosPasivos` + dashboard) — suite **589/589**, tsc limpio. **Fase F COMPLETADA (2026-08-30):** UI + i18n del write-off — botón `WriteOffButton` (danger + ConfirmDialog) → `writeOffCreditAction`; badge "Incobrable"; card castigada; ocultamiento de acciones (editar/abono/borrar) cuando writtenOff; filtro de estado "Incobrable" (writtenOff) excluido de Pendiente/Pagado; columnas Capital/Interés en detalle de abonos; i18n es/en (7 claves nuevas). Suite **589/589**, tsc limpio. **Fase G COMPLETADA (2026-08-30):** QA final — `pnpm build` exitoso, suite **589/589**, tsc limpio; docs cerradas (estado, bitácora, protocolo post-compactación); `AGENTS.md` actualizado (principio financiero 7: amortización capital primero + write-off). Push `68eb9c6`. **RONDA 9 COMPLETADA.** |
| **Ronda 10 — 5 solicitudes de producto del usuario** | ✅ **COMPLETADA (2026-08-30).** Auditoría completada (solicitudes 1–5 analizadas) y plan por fases (A–F) aprobado por el usuario. **Fase A ✅** `1a27cf0 fix(i18n)`: toasts de error de eliminación traducidos y descriptivos — `handle-action-error.ts` mapea 6 mensajes de borrado a claves `error.*` descriptivas, `entity-delete-button.tsx` usa `useT('error')` + strip del prefijo, +6 claves es/en, test nuevo (11 tests). **Fase B ✅** `728308f feat(dashboard)`: card "Flujos de financiamiento" muestra "Créditos recibidos" y "Créditos otorgados" en dos líneas (sin neto grande), solo UI + +2 claves i18n. **Fase C ✅** `7295f91 fix(transfers,dashboard)`: mensaje de fondos insuficientes de transferencias corregido y traducido (helper `use-action-error.ts`) + saldo de cuenta negativo en rojo. **Fase D ✅** `256c5e7 feat(validation)`: fechas futuras rechazadas en UI (`max` = hoy) y backend (movements, transfers, credits, payables, pos/sales) + claves i18n. **Fase E ✅** `d10c54b feat(sales)`: crear cliente y artículo desde Nueva venta — modales anidados con auto-selección; `createClientAction`/`createCatalogItemAction` devuelven snapshot serializado para que el form de venta seleccione la entidad creada; 2 tests nuevos por action. **Fase F ✅**: QA final — suite **614/614** (57 archivos), tsc y build limpios; docs cerradas (estado, bitácora). Push `5621616..fc5b03d` master → origin. **⚠️ DEPLOY FALLIDO según GitHub (reportado por el usuario 2026-08-30): la versión desplegada NO tenía la UI de Fase E (síntoma: modales de cliente/artículo ausentes).** Al momento de escribir, la causa del fallo no es visible desde el entorno local (repo privado, sin `gh` CLI, sin logs de CI en el repo — no hay `.github/workflows`, `vercel.json` ni `netlify.toml`; el push fue a `https://github.com/pabadev/twincap`). Re-despliegue pendiente tras la Ronda 11. |
| **Ronda 11 — Funcionalidades backend sin UI (4 casos) + re-deploy** | ✅ **IMPLEMENTADA (2026-08-30).** Auditoría previa: 4 funciones con backend listo pero sin UI. **Caso 1** editar cliente: `updateClientAction` ahora comparte envelope `ClientActionResult` con `createClientAction` (devuelve snapshot `client.toJSON()`), `client-form.tsx` con modo edición + botón lápiz en `clients-list.tsx` (modal `Clients.editTitle`), +3 tests. **Caso 2** renombrar cuenta: `updateAccountAction` + `rename-account-button.tsx` (modal con Input, `Accounts.renameTitle`), botón en `accounts/page.tsx` para todas las filas, revalida `/accounts /dashboard /movements /transfers`, +3 tests. **Caso 3** renombrar categoría: `updateCategoryAction` + `rename-category-button.tsx` (modal `Categories.editTitle`), botón en `categories/page.tsx`, revalida `/categories /movements`, +3 tests. **Caso 4** editar transferencia: `updateTransferAction` (usa case `updateTransfer`, sin tocar cuentas — TRA-5), `transfer-form.tsx` con modo edición (selects bloqueados, monedas inicializadas, `mirroredDest` para misma moneda, fecha vía `businessDateToInputValue`), botón lápiz en `transfers-list.tsx` (modal `Transfers.editTitle`), revalida `/transfers /accounts /dashboard /movements`, +3 tests. i18n es/en: +11 claves (renameTitle/editTitle/updating/updateClient/updateTransfer/transferUpdated). Suite **625/625** (59 archivos), tsc EXIT 0, build EXIT 0. Pendiente: commits por unidad, push y verificación de re-deploy. **Postscript (2026-08-30/31):** deploy de R11 OK, pero los modales anidados de Fase E no hacían submit — un `<form>` no puede contener otro `<form>` (los `Modal` de `ClientForm`/`CatalogForm` vivían dentro del form de la venta; el navegador ligaba sus controles al form padre). Fix `e3b31b6 fix(sales)`: ambos modales movidos fuera del árbol del form (hermanos), botones `type="button"` que los abren quedan dentro. Único archivo afectado (verificado por grep <form+<Modal). Suite 625/625, tsc, build. Push `6dd576c..e3b31b6`. |

| **Ronda 12 — Production & Commercial Hardening** | 🟢 **COMPLETADA (2026-09-02)** — **FASE A COMPLETADA (2026-08-31)**, **FASE B COMPLETADA (2026-08-31)**, **FASE C-1 COMPLETADA (2026-08-31)**, **FASE C-2 COMPLETADA (2026-08-31)**, **FASE C-3 COMPLETADA (2026-09-02)**, **FASE C-4 COMPLETADA (2026-09-02)** — basadas en la auditoría externa `docs/TwinCap - Evaluación 2 ChatGPT.md` (30/08/2026) provista por el usuario como base de diseño. Foco: seguridad, fundamentos de negocio, robustez, eficiencia y escalabilidad comercial — NO features nuevas. Fases A–C definidas en sección R12. **A1 rate limiting auth** y **A2 idempotencia client-provided** (suite 637/637; commits `4a2a3ac`, `0826855`, `a803902`). **B1 tenant isolation (38 tests) + B2 consistencia + `docs/CONSISTENCY-AUDIT.md` + fixes `deleteCreditReceived`/`deletePayable`/`updateClient`** (suite 677/677; commit `86c7a30`). **C1 dashboard eficiente**: agregación server-side reactiva — el cliente deja de recibir la lista completa de movimientos; recibe un snapshot agregado + últimos 5 (suite 690/690). **C2 observabilidad/audit trail**: `OperationLogger` (ports) + `OperationLog`/`MongoOperationLogger` (infra) + wrapper `withAudit` — 37 operaciones financieras críticas + auth instrumentadas; registro mínimo durable en Mongo sin PII; best-effort sin transacciones (mutación primero, log después, señal stderr). Suite **709/709**, tsc limpio. **Fix extra (fuera de C2, aprobado):** test `build-dashboard-snapshot.test.ts` era date-stale (fixtures fijos en ago-2026 → roto al pasar a sep-2026); ahora derive fechas del reloj real. **C3 E2E Playwright**: suite de 24 flujos críticos aditiva (zero src/), entregada como 4 PRs stacked-to-main mergeados a master. **C4 limpieza identidad `globalmoney` → TwinCap**: commit `fff19b3` (package.json + referencias obvias; docs de auditoría conservadas como evidencia de P10). Detalle sección R12 / bitácora 2026-08-31 y 2026-09-02. **RONDA 12 CERRADA.** |
| **Ronda 13 — Beta Readiness & Product Validation** | 🟡 **EN CURSO (2026-09-02)** — basada en la auditoría externa `docs/Auditoría Pre-Beta ChatGPT.md`. Foco: convertir el software en un servicio **operable, legalmente preparado y medible** para una beta cerrada con usuarios reales (10–20), y **preparar la base de multiusuario (Workspace + Membership)** sin activar aún la funcionalidad Teams visible. Incluye **cambio de regla explícito y justificado**: de tenant basado en `userId` a tenant basado en `workspaceId` (autorizado por el usuario; el single-user queda como caso degenerado de un workspace personal). NO implementa Teams/Billing/Compras (post-beta). Plan por fases A–H en sección R13. **FASE A COMPLETADA (2026-09-02): CI automática GitHub Actions** — `.github/workflows/ci.yml` (jobs `quality` typecheck+lint+vitest+build, y `e2e` Playwright serial) + **limpieza del lint del repo** (estaba roto: 86 problemas/55 errores → 0/0: refactor `idempotency-field` a lazy useState, `SortIcon` fuera del render, disables puntuales justificados en server-action/theme-hydration, tipado de `any` en tests, override `no-require-imports` para `.cjs`, limpieza de warnings). Suite **709/709**, `tsc --noEmit` EXIT 0, `lint` EXIT 0. **FASE B COMPLETADA (2026-09-02).** **FASE C COMPLETADA (2026-09-03)** — 4 páginas legales bilingües `/privacy`, `/terms`, `/cookies`, `/data-policy` + contenido `src/content/legal/` + enlaces desde landing/auth; suite **722/722**, tsc/lint/build EXIT 0; commit `71865eb`. **FASE D COMPLETADA (2026-09-03)** — error monitoring + alertas (commits `6bd9b77` + `22a933c`, suite **750/750**). **FASE E COMPLETADA (2026-09-03)** — feedback en producto + centro de ayuda `/help` (commit `507cf61` + push `682a016..507cf61` junto al commit de emails `2daf633`). **FASE F COMPLETADA (2026-09-03)** — Workspace foundation multiusuario estructural (el cambio de regla R13.2). **F1** dominio `Workspace` + `Membership` (commit `2d0a372`, +tests). **F2** auth: derive `workspaceId` en sesión + alta de Workspace/Membership(owner) al registrarse (commit `bd5e4e1`). **F3** migración de las 10+ entidades financieras de `userId` → `workspaceId` como frontera de aislamiento, preservando `userId` como actor (commit `1d599ce`). **F4** rework de la suite de tenant isolation a semántica workspace + compartición same-workspace (commit `6a0f156`, ~+296/−69). **F5** backfill: cada usuario existente recibe 1 Workspace personal (`_id` = su `userId`) + 1 Membership owner; renombra `userId`→`workspaceId` en las 10 colecciones financieras — idempotente, 3 tests (commit `ba36036`). **AGENTS.md** actualizado (R13.2.4): regla de tenant pasa a `workspaceId` con aislacion por Workspace, español neutro (commit `48cbe28`). Suite **791/791** (77 archivos), `tsc --noEmit` EXIT 0. **Ejecución de producción (Atlas, DB `globalmoney`):** deploy `8a31462` (Vercel OK) + migración ejecutada — 12 workspaces + 12 memberships(owner), 383 docs financieros renombrados `userId`→`workspaceId` (0 restantes), índice único de categorías recreado como `workspaceId_1_name_1_type_1`. ⚠️ En la primera corrida en producción se descubrió un defecto crítico NO cubierto por tests: el índice único `userId_1_name_1_type_1` de categorías rompía el `$rename` (E11000 por duplicados `userId:null` entre usuarios). Fix commiteado (`8974236`): se dropean los índices `userId`-led antes del rename y se recrea el constraint único scoped a `workspaceId`; +test de regresión E11000. Suite **791/791** (76 archivos), `tsc --noEmit` EXIT 0, deploy `8974236` OK. **FASE G COMPLETADA (2026-09-04): Analítica de producto** — evento `AnalyticsEvent` sin PII + puerto `AnalyticsReporter` + repo best-effort; eventos instrumentados en 6 server actions detrás del flag opt-in `ANALYTICS_ENABLED`; página `/analytics` con activation/retention/usage. Suite **801/801** (+10 tests), tsc/lint/build EXIT 0. **Ronda 13 EN CURSO — siguiente: Fase H (Beta QA, Definition of Beta y lanzamiento).** |
| **Ronda 14 — Confiabilidad financiera y cierre pre-beta** | 🟡 **EN CURSO (2026-09-06).** Basada en la auditoría externa `docs/audit_pre_ronda_14.md` (Auditoría 2.0). Foco: **confiabilidad financiera** (moneda, atomicidad, rate limiter real), hardening de auth/monitor/CI, corrección de analytics/dashboard y cierre de los bloqueos de beta (backup/restore real, smoke test, legal, aislamiento). Fase 0 (higiene documental: este archivo + histórico) **COMPLETADA**; Fase D (Next.js 16.3.4) **COMPLETADA (2026-09-06)** — suite 919/919, tsc/lint/build EXIT 0; Fase E (configuración de producción) **COMPLETADA (2026-09-06)** — suite **929/929**, tsc/lint/build EXIT 0; **Fase A (integridad de moneda) COMPLETADA (2026-09-06)** — 30 archivos, suite **958/958** (88 archivos), tsc/lint/build EXIT 0; cierre del riesgo residual del writer (abono desde cuenta de moneda distinta a la deuda ahora rechazado). **Hotfix post-fase:** el guard legacy de catálogo rompió Catálogo/Venta POS con los 19 items reales sin `currency`; backfill `scripts/backfill-catalog-currency.mjs` ejecutado contra Atlas (19 items/7 workspaces → COP, semántica legacy replicada), verificado 0 sin currency. **Fase B (atomicidad financiera) COMPLETADA (2026-09-06)** — transacciones reales (M0 de Atlas ES replica set, verificado empíricamente): port `UnitOfWork` + `TransactionHandle` opaco + `MongoUnitOfWork` con `session.withTransaction`; `createTransfer` y `createSale` envuelven su fase de escritura completa; 3 tests de integración de rollback REAL contra `MongoMemoryReplSet` + 2 unit de rollback; 29 call sites con `fakeUow()`; suite **963/963** (89 archivos), tsc/lint/build EXIT 0. **Fase C (rate limiter real) COMPLETADA (2026-09-06)** — IP real desde `headers()` (antes `_ip` que el frontend nunca enviaba → bucket `:unknown` compartido); limiter atómico `findOneAndUpdate`+`$inc`+índice único + retry E11000; monitor con gate 120/15min; 3 tests de concurrencia real §25-C (40 checks → exactamente 5 allowed); suite **978/978** (92 archivos), tsc/lint/build EXIT 0. **Operativo ejecutado (2026-09-06):** migración de índice único de `ratelimits` contra Atlas (`scripts/migrate-rate-limit-unique-index.mjs`, commit `f6b9946`) — 0 duplicados en prod, `key_1` único creado, TTL preservado. **Fase F (auth hardening) COMPLETADA (2026-09-06)** — consumo atómico de tokens one-time (`consume(tokenId)` condicional con `modifiedCount===1`; N=40 concurrentes → 1 winner) + `sessionVersion` que invalida sesiones tras password change/reset (`getCurrentUser` valida siempre; `changePasswordAction` borra cookie actual); suite **992/992** (93 archivos), tsc/lint/build EXIT 0; commit `dba3369` pusheado. **Fase G (protección `/api/monitor`) COMPLETADA (2026-09-07)** — guard anti-spike §6: (1) cupo de **30 fingerprints únicos por IP/15 min** (modelo `monitor-fingerprint` con `$addToSet` condicional atómico; repetidos no consumen cupo; 40 concurrentes → exactamente 30 admitidos) + (2) **cooldown global de 10 min** cuando >30 fingerprints NUEVOS en 5 min (modelo `monitor-cooldown` singleton, `$max` extiende TTL para lectura del cooldown activo) + (3) **throttle de alertas**: máx. 1 email por fingerprint/30 min vía `monitorAlertRateLimiter` (check-and-consume, sin modelo nuevo). Ruta ordena: `connectDb → rateLimit(120) → checkGlobalCooldown (429) → zod (400) → computeFingerprint → limitFingerprints (429 fingerprint_quota) → registerGlobalFingerprint si isNew → reportError`; fail-safe intacto (nunca 500). Guard fail-open por contrato (guarda rota no bloquea reportes legítimos). Suite **1007/1007** (94 archivos), tsc/lint/build EXIT 0 — **NUEVOS modelos/colecciones** `monitorfingerprints` y `monitorcooldowns` (TTL+unique vía autoIndex, sin migración manual). **POST-G (2026-09-07) dos defectos corregidos por decisión del usuario ("resuelve G primero"):** (1) **throttle de alertas fail-OPEN → fail-CLOSED** — con Mongo caído, `reporter.report` devolvía `{isFirst:true}` fabricado y el cooldown DB-backed fallaba open → SPAM de emails; ahora `withAlertCooldownTimeout` (1 s, inyectable `alertCooldownTimeoutMs`) + catch fail-closed suprimen la alerta con log `monitor_alert_suppressed` (perder una alerta puntual es aceptable; cien por segundo de backend muerto no). (2) **carrera `$addToSet` no-op contada como `isNew:true`** en `limitFingerprints` — dos concurrentes con el mismo fingerprint nuevo inflaban el contador global → cooldown prematuro; ahora `updateOne` + `modifiedCount` distingue el add real (1) del no-op (0, repeat legítimo). Test nuevo de concurrencia N-same-fingerprint → exactamente 1 isNew:true (fallaba antes del fix, verificado). Suite **1024/1024** (95 archivos), tsc/lint/build EXIT 0. **AUDITORÍA 2026-09-07 (feedback del auditor sobre la implementación):** aprueba el diseño ("muy bien planteada") pero exige tratar los índices como **contrato de infraestructura**, no como optimización secundaria — el `unique` de `key_1` participa en la garantía anti-race (E11000 → retry: dos registros concurrentes NO duplican bucket) y el TTL decide cuándo desaparece el estado. **VERIFICACIÓN P0 EJECUTADA contra el cluster productivo 2026-09-07 (read-only):** `monitorfingerprints` y `monitorcooldowns` **NO EXISTÍAN en Atlas** tras el deploy (el autoIndex de Mongoose solo materializa los modelos cuando la app los carga en runtime — serverless los carga lazy) → el contrato NO podía depender del arranque de la app. **RESUELTO:** nuevo `scripts/ensure-monitor-indexes.mjs` (patrón `migrate-rate-limit-unique-index.mjs`, dry-run/`--apply`, idempotente, no destructivo, 0 credenciales) ejecutado contra Atlas con autorización explícita del usuario: creó ambas colecciones vacías + los 4 índices (`key_1` unique + `expiresAt_1` TTL 0 en cada una) ANTES de que `/api/monitor` reciba tráfico. Verificador `scripts/verify-monitor-indexes.mjs` post-apply: **`CONTRACT OK`** (2 colecciones + 4 índices con propiedades reales verificados directo contra Atlas, sin depender de Next.js/Mongoose); re-dry-run idempotente: 4× `[PASS] no-op`. Semántica temporal documentada (por qué 30/15min, 30/5min, 10min, 1 email/30min — P1 del auditor, sección 7.1). **CRITERIO DE CIERRE G:** los índices de seguridad existen en Atlas ANTES de que `/api/monitor` reciba tráfico; `autoIndex` queda únicamente como redundancia para entornos locales/desarrollo. **Fase H (corrección de analytics) COMPLETADA (2026-09-07)** — §8 flux real: evento REGULAR `movementCreated` + taxonomía completa por módulo (`transferCreated`, `creditReceivedCreated`, `creditGrantedCreated`, `payableCreated`; ampliación pedida por el usuario "integra más eventos relevantes pero bien implementados"); 5 server actions instrumentadas (movements/transfers/créditos recibidos/otorgados/payables); activation = % registered con ≥3 `movementCreated` en 2 días (antes inalcanzable por dedup de `firstMovement`); `avgMovementsPerUser` = `totalMovementsCreated/totalRegistered`; §9 página `/analytics` 100 % i18n es/en (`getT('Analytics')`, namespace nuevo, 15 cards). Suite **1018/1018** (95 archivos), tsc/lint/build EXIT 0. **Pendiente deploy:** activar `ANALYTICS_ENABLED=true` en producción. Plan completo por fases 0 + A–O en sección R14. **ESPERANDO APROBACIÓN para la siguiente fase (I — dashboard: límites de carga) (protocolo R3.13).** | **Fase I (dashboard: límites de carga) COMPLETADA (2026-09-07)** — lectura por ventana real + índice compuesto `workspace_date_createdAt`; balance con histórico completo y proyección mínima (cifras idénticas a suite existente, Opción A aprobada); 23 archivos; suite **1041/1041** (96 archivos), tsc/lint/build EXIT 0. **Fase J (CI/devops) COMPLETADA (2026-09-07)** — trigger `master`, pnpm-only, Node 24, typegen previo al typecheck, env E2E con placeholders; CI real en verde (Quality + E2E 24 passed); dominio productivo twincap.vercel.app establecido + CLI de Vercel configurado (deploy automático por push a master). **Fase N (mapper round-trip + capa de testing §25) COMPLETADA (2026-09-07)** — fix `movement.ts` persiste/reconstruye `link.saleId`; §25-D con `movement-mapper.roundtrip.test.ts` (4 tests, MongoMemoryServer + modelo real); cobertura §25 completa (E = mojón staging); fix flakiness bcrypt; suite **1045/1045** (97 archivos), tsc/lint/build EXIT 0; Fase K (arquitectura: puertos `WorkspaceBootstrapper`/`SessionCookieManager` + tipos de dashboard a core + `findByIdRaw` scoped) **COMPLETADA** — suite **1048/1048** (98 archivos); **Fase L (feedback persistente) COMPLETADA (2026-09-07)** — §11: port `FeedbackRecord`/`FeedbackStore` + modelo `Feedback` (estados delivered/failed) + repo best-effort; el dispatcher distingue `delivered`/`not_configured`/`transport_error`; la acción persiste el resultado REAL y solo dice "enviado" cuando el email salió (dev sin provider → no-op silencioso sin persistir, nunca error falso); suite **1070/1070** (102 archivos), tsc/lint/build EXIT 0; **Fase M (cookie rename) COMPLETADA (2026-09-07)** — §21: `COOKIE_NAME` `gm_session` → `twincap_session` (`session-cookie.ts:4`), páginas legales es/en actualizadas, E2E actualizado (logout limpia la cookie nueva, aislamiento de tenants), +4 tests unitarios nuevos de la constante; suite **1074/1074** (103 archivos), tsc/lint/build EXIT 0, E2E **24/24**; efecto aceptado: invalidación única de sesiones activas (pre-beta). | |

## PENDIENTES OPERATIVOS HEREDADOS (cerrados en R13, pendientes de ejecución/operación)

1. **R13-H1 — Checklist de aislamiento en producción**: `docs/beta-qa-isolation-checklist.md` sin ejecutar por el fundador (requiere 2 cuentas). → Ronda 14, Fase O.
2. **R13-G/hardening — Analytics**: activar `ANALYTICS_ENABLED=true` y setear `ANALYTICS_EXCLUDE_EMAILS=fjpaba1989@gmail.com` en Vercel cuando se quiera empezar a medir (decisión del fundador).
3. **R13-H2 — Definition of Beta**: Grupo B con Backup/restore (P0-c) y smoke test (P0-b) abiertos. → Ronda 14, Fases J/O.
4. **R13-C — Legal**: placeholders `[RAZÓN SOCIAL]`, `[NIT]`, `[DIRECCIÓN]`, `[CORREO DE CONTACTO]`, `[CIUDAD/PAÍS]` por reemplazar; revisión con abogado antes de beta comercial. → Ronda 14, Fase O.
5. **Segunda oleada — Backburner**: "leyenda de SOL", ajustes igl/insight y hoja de ruta futura siguen en pausa deliberada (decisión del usuario 2026-09-04). La herramienta prettier está instalada como devDependency pero NO se aplicó formateo masivo (estado medido, sin `--write`).
6. **R14-C — Migración índice único `ratelimit` en Atlas**: ✅ **EJECUTADA (2026-09-06).** `scripts/migrate-rate-limit-unique-index.mjs` (commit `f6b9946`): dry-run confirmó **0 keys duplicadas** en producción (la carrera `findOne→create` nunca se materializó), luego `--apply` dropeó el índice `key_1` no-unique y creó `{key:1}` único; TTL `expiresAt_1` preservado. Verificado post-migración: `key_1` único activo.

## REGLAS INQUEBRANTABLES (heredadas de R4.3)

### De Ronda 3 / AGENTS.md (todas vigentes)
- Arquitectura hexagonal intacta
- `connectDb()` antes de repositories
- Multi-tenancy verificado en backend
- i18n es/en paridad, español neutro
- Principios financieros (transferencia ≠ ingreso/gasto, saldo ≠ resultado, etc.)
- Máximo UNA dependencia nueva por fase (justificada)
- Sin hook GGA
- Fechas: sin offsets ±1 día sin causa raíz
- pnpm exclusivo
- Sin window.location.reload()

### Nuevas de R4 (del documento fuente)
- **No volver a introducir `Account.scope`** — Movement.context es la fuente de Personal/Negocio (decisión D3-bis de R3)
- **No cambiar la semántica de Activos/Pasivos** — independientes de filtros de actividad (estado R3)
- **No inventar columnas** en las tablas de resumen: 4 columnas máximo, justificadas por utilidad
- **No mostrar todos los reportes simultáneamente** — menú de acceso, no listado completo
- **No convertir monedas** — agrupar por moneda de forma honesta
- **No contar transferencias como ingreso/gasto** — conservar exclusión de F1
- **Conservar saludo, filtros, Activos/Pasivos, evolución anual, cards, balances, dark mode, i18n, responsive, loading/empty/error states, accesibilidad, navegación, seguridad**


> **Protocolo R3.13 (resumen):** cada fase comienza con auditoría (nunca código), se presenta el plan definitivo por fases para aprobación explícita del usuario, y cada fase se implementa SOLO con su aviso explícito. Detalle completo en `docs/AUDIT-AND-PLAN-HISTORY.md` (sección Ronda 3, R3.13).

---

# RONDA 14 — CONFIABILIDAD FINANCIERA Y CIERRE PRE-BETA

Iniciada: 2026-09-06 · Fuente base de diseño: **auditoría externa** `docs/audit_pre_ronda_14.md` (Auditoría 2.0, provista por el usuario como insumo posterior al cierre de R13). `AUDIT-AND-PLAN.md` sigue siendo el plan maestro; la auditoría aporta los hallazgos priorizados que esta ronda implementa.

## R14.1 — Contexto y objetivo

La Auditoría 2.0 cambia ligeramente el diagnóstico respecto a la conclusión optimista post-R13: no falta funcionalidad, falta **confiabilidad**. Su veredicto (§26): beta privada de 2–3 usuarios técnicos 🟠 **sí, con supervisión intensa**; beta privada de 10–20 usuarios 🔴 **todavía no**; beta pública 🔴 **no**; SaaS comercial 🔴 **no**. El diagrama de madurez (§27) lo resume: Auth 🟢, QA 🟢, SaaS 🟠, Tenant 🟢, Observabilidad 🟢, **Integridad financiera 🔴**.

Directiva del usuario (2026-09-06): planificar la Ronda 14 atendiendo **todos** los hallazgos — soluciones de fondo para los críticos y sin dejar ninguno afuera, incluidos los menores — dividida en suficientes fases para un alcance grande y complejo, respetando las reglas del proyecto.

La auditoría confirma además una lección de testing (§24): **849 tests no equivalen a 849 garantías**. Encontró un defecto real invisible para la suite: `link.saleId` definido en el dominio (R13 F4) pero **ni persiste ni reconstruye** `src/infrastructure/mappers/movement.ts`. La razón: la suite prueba `use case → fake repository`, no `domain → mapper → mongoose → mapper → domain`. La próxima capa de testing debe concentrarse en **persistencia real + invariantes + fallos parciales + entradas hostiles** (§25).

## R14.2 — Auditoría de verificación (2026-09-06, solo lectura)

> **Nota de vigencia:** la Auditoría 2.0 se elaboró sobre un ZIP del repositorio; sus números de suite/fechas pueden no coincidir con `master`. La verificación de esta sección se hizo contra `master` al 2026-09-06 (post-Fase 10 de la segunda oleada: suite **919/919** en 87 archivos, `tsc --noEmit` EXIT 0, `lint` EXIT 0, `build` EXIT 0; último commit pusheado `11366a7`).

Todos los hallazgos se contrastaron contra `master` con evidencia `file:line`. Resultado: **todos reales** (ninguno inventado ni obsoleto).

| ID | Hallazgo (auditoría §) | Veredicto en master (evidencia) | Fase |
|---|---|---|---|
| §3 | El backend no protege realmente la moneda de la cuenta | ✅ REAL — el repo de movimientos reconstruye la moneda desde la cuenta viva al leer (`movement-repository.test.ts:133`); el catálogo NO almacena `currency` (reconstrucción vía `resolveAccountCurrency` en `catalog-repository.ts`); `create-transfer.ts` acepta cross-currency validando solo la forma (source ≠ destination; D3 resuelve cuentas) | A |
| §4 | Operaciones financieras no atómicas | ✅ REAL — operaciones multi-documento sin transacción (inventario documentado en `docs/CONSISTENCY-AUDIT.md`) | B |
| §5 | Rate limiting de auth no usa la IP real | ✅ REAL — `(auth)/actions.ts:78` lee `formData.get('_ip') || 'unknown'`; claves `register:${ip}`; `rate-limiter.ts` NO atómico (`findOne` → `attempts++/save` → `create`) | C |
| §6 | `/api/monitor` puede abusarse | ✅ REAL — validación zod estricta (message≤500, name≤100, stack≤4000) pero **sin** rate limit ni throttle por fingerprint | G |
| §7 | El dashboard "eficiente" carga todos los movimientos en backend | ✅ REAL — `build-dashboard-snapshot.ts` agrega sobre el set completo por ventana | I |
| §8 | Activation no representa lo que promete | ✅ REAL — solo eventos "first" deduplicados (`firstLogin`/`firstMovement`); sin evento regular por movimiento | H |
| §9 | Analytics viola la regla explícita de i18n | ✅ REAL — `src/app/(main)/analytics/page.tsx` hardcodea "Analytics Dashboard", "Product metrics…", "Registered", "Logged In", "First Movements" | H |
| §10 | Email de producción puede apuntar a localhost | ✅ REAL — `env.ts`: `RESEND_API_KEY`/`RESEND_FROM`/`APP_BASE_URL` opcionales; `auth-email-deps.ts:25` → `baseUrl: env.APP_BASE_URL ?? 'http://localhost:3000'` | E |
| §11 | Feedback puede decir "enviado" sin enviarse | ✅ REAL — `feedback-alerter.ts` (patrón fail-safe de `error-alerter`) nunca lanza; `submitFeedbackAction` responde ok aunque el transporte falle; sin persistencia | L |
| §12 | Tokens one-time con ventana de concurrencia | ✅ REAL — `auth-token-repository.ts` `markUsed` vía `updateMany`; la ventana real es el hueco find→use del use case | F |
| §13 | Cambiar la contraseña no invalida sesiones anteriores | ✅ REAL — sin `sessionVersion` en usuario/sesión | F |
| §14 | Pequeñas violaciones de las reglas de arquitectura | ✅ REAL — `register.ts:7` importa `../../../infrastructure/seeding/user-bootstrap`; `logout.ts:1` importa `../../../infrastructure/auth/session-cookie`; `build-dashboard-snapshot.ts:1–3` importa tipos de `../../../components/dashboard/*` | K |
| §15 | `findByIdRaw()` rompe parcialmente la pureza del aislamiento | ✅ REAL — `src/core/domain/repositories.ts:97` (Transfer) + `transfer-repository.ts:68` (ignora `workspaceId`) | K |
| §16 | Dependencias: actualizar Next.js antes de beta | ✅ REAL — `next 16.3.1` + `eslint-config-next 16.3.1` (CVEs corregidos en 16.3.4) | D |
| §17 | CI tiene dos detalles que corregir | ✅ REAL — `.github/workflows/ci.yml` dispara en `[main]` (repo usa `master`); usa `npx playwright install` (viola pnpm-exclusivo) | J |
| §18 | Backup/restore sigue siendo un bloqueo real | ✅ REAL — Atlas M0 sin backups automáticos; sin restore probado (P0-c de Definition of Beta sigue abierto) | O |
| §19 | Checklist manual de aislamiento sigue obligatorio | ✅ REAL — `docs/beta-qa-isolation-checklist.md` existe sin ejecutar por el fundador | O |
| §20 | Legal existe pero no está listo para producción | ✅ REAL — páginas con placeholders `[RAZÓN SOCIAL]`, `[NIT]`, `[DIRECCIÓN]`, `[CORREO DE CONTACTO]`, `[CIUDAD/PAÍS]`; revisión con abogado pendiente | O |
| §21 | Inconsistencia de branding residual | ✅ REAL — `session-cookie.ts:4` `COOKIE_NAME = "gm_session"`; las páginas legales la nombran; residuos `gm_*` | M |
| §24 | Defecto invisible para los tests: `link.saleId` no se persiste ni reconstruye | ✅ REAL — `src/infrastructure/mappers/movement.ts` escribe/lee solo `kind/refId/opId`; `saleId` (agregado a dominio + schema en R13-F4) se pierde en el round-trip | N |

Verificados como reales además: el acceso a `/analytics` restringido por política `AnalyticsAuthorizer` (R13-G hardening) y la **exclusión del owner pendiente en Vercel** (`ANALYTICS_EXCLUDE_EMAILS=fjpaba1989@gmail.com`); `'Unauthorized'` queda SOLO en `handle-action-error.ts` (Fase 8 segunda oleada); rate limiters también presentes en profile (`password:${userId}`, `resendVerify:${userId}`).

## R14.3 — Plan por fases (PROPUESTA — Fase 0 APROBADA y ejecutada; el resto REQUIERE APROBACIÓN)

Siguiendo el protocolo R3.13: auditoría → plan → aprobación → fase → tests → docs → detenerse. Cada fase se implementa SOLO con aviso explícito del usuario.

| Fase | Contenido | Hallazgos | Dependencias |
|------|-----------|-----------|:---:|
| **0 — Higiene documental** | ✅ **APROBADA Y EJECUTADA (2026-09-06).** Rondas 1–13 + bitácora archivadas en `docs/AUDIT-AND-PLAN-HISTORY.md` (testimonio verbatim). Maestro compactado: ESTADO ACTUAL (R1–R13 + R14), pendientes operativos heredados, reglas inquebrantables (R4.3), plan R14, protocolo post-compactación. | — | — |
| **A — Integridad de moneda** | ✅ **COMPLETADA (2026-09-06).** 30 archivos. (1) Movements: `create-movement` rechaza `input.currency !== account.currency`; `update-movement` re-valida si cambia `accountId`. (2) Transfers: `create/update` derivan las monedas de las cuentas reales (rechazan declaradas distintas) y exigen rate explícito solo en cross-currency (R3.14). (3) Catálogo persiste `currency` (`models/catalog.ts` + `mappers/catalog.ts` + `catalog-repository.ts`); eliminado `resolveAccountCurrency`; `update-catalog-item` moneda inmutable; guard defensivo en el mapper: docs legacy sin `currency` fallan con `MoneyError` claro. **Backfill ejecutado contra Atlas (2026-09-06):** `scripts/backfill-catalog-currency.mjs` — 19 items legacy en 7 workspaces recibieron `currency` COP (semántica legacy replicada: moneda de la primera cuenta del workspace; todas las cuentas eran COP). Verificado 0/19 items sin currency. (4) Venta POS: `create-sale` valida las 3 monedas (artículo catálogo vs cuenta de cobro vs venta); `add-sale-abono` valida vs moneda de la cuenta de la venta. (5) Créditos recibidos/otorgados + payables: create valida contra cuenta; `edit-principal`/`edit-total` moneda inmutable; `add-abono` valida moneda de la deuda Y moneda de la cuenta de pago (riesgo residual del writer cerrado: el movimiento se re-etiquetaba en la moneda de la cuenta al leer). (6) Tests: 9 adversariales §25-A del writer + 3 nuevos (cuenta de pago con moneda distinta) + 5 nuevos de mapper catálogo (round-trip + legacy). Suite **958/958** (88 archivos), `tsc --noEmit` EXIT 0, `lint` EXIT 0, `build` EXIT 0. | §3 | 0 |
| **B — Atomicidad financiera** | ✅ **COMPLETADA (2026-09-06).** **Transacciones reales** sobre las 2 ventanas crudas del inventario (decisión R14.6 resuelta: M0 de Atlas ES replica set y soporta transacciones — verificado empíricamente). Contrato hexagonal: `TransactionHandle` opaco con symbol brand (`core/domain/transaction.ts`) + port `UnitOfWork.withTransaction` (`core/application/ports.ts`); adaptador `MongoUnitOfWork` (`infrastructure/transactions/mongo-unit-of-work.ts`) usa `session.withTransaction()` (retry automático de `TransientTransactionError`); los 5 repos Mongo afectados aceptan `tx?` opcional y pasan `{ session }` (`TransferRepository.create`, `MovementRepository.create`, `SaleRepository.create`, `CreditGrantedRepository.create`, `CatalogItemRepository.decrementStock`). `createTransfer` envuelve sus 3 writes; `createSale` envuelve la fase completa (stock + sale + movements + credit); validaciones/lecturas quedan FUERA de la transacción. Server actions inyectan `new MongoUnitOfWork()`. Tests: 3 de integración de rollback REAL contra `MongoMemoryReplSet` (abort total en fallo parcial §25-B, commit total en éxito, propagación + reuso) + 2 unit de rollback en los use cases; 29 call sites actualizados con `fakeUow()`. Suite **963/963** (89 archivos), `tsc --noEmit` EXIT 0, `lint` EXIT 0, `build` EXIT 0. Los deletes NO se tocaron (ya idempotentes + tolerantes). | §4 | 0, C (tests concurrentes para verificar) |
| **C — Rate limiter real** | ✅ **COMPLETADA (2026-09-06).** IP real desde `headers()` (`x-real-ip` → primer `x-forwarded-for`), no más `formData.get('_ip')` (el frontend nunca lo enviaba → todas las claves eran `:unknown`, bucket compartido entre todos los usuarios). Nuevo helper puro `src/infrastructure/auth/client-ip.ts` (`resolveClientIp` sanitiza `::ffff:`, puertos IPv4/IPv6, trunca a 64; `getClientIp` envuelve `headers()` async de Next 16). Limiter atómico: `findOneAndUpdate` + `$inc` sobre el doc activo; sin ventana activa → `deleteOne` condicional (solo stale) + `create` con retry E11000; índice **único** en `key` (antes `index`). Aplicado a auth (register/login/forgot con IP real; claves intactas), profile hereda el limiter atómico (misma instancia) y **monitor** gana gate grueso por IP (120/15min → 429; Fase G añade fingerprint+cooldown). 3 tests de concurrencia REAL contra `MongoMemoryServer` (§25-C): 40 checks concurrentes → exactamente maxAttempts (5) allowed, attempts = set 1..40 sin pérdidas/duplicados; ventana stale reanuda en 1; reset limpia. Suite **978/978** (92 archivos), tsc/lint/build EXIT 0. **Pendiente operativo:** migración de índice único en Atlas — dedupe de `ratelimit` antes de crear el índice (la carrera del código viejo pudo dejar keys duplicadas; `Mongoose autoIndex` fallaría con E11000). | §5 | 0 |
| **D — Next.js 16.3.4** | ✅ **COMPLETADA (2026-09-06).** `next` + `eslint-config-next` 16.3.1 → 16.3.4. CVEs cubiertos (security release 2026-08-25, 16.3.3 + follow-up 16.3.4): GHSA-p293-qw3h-jr36 / CVE-2026-75604 — RCE sin autenticar por path traversal en servidores Windows (crítico); GHSA-2xp9-vwfh-vxw4 — RCE sin autenticar en Image Optimization con AVIF (crítico, libheif via sharp; AVIF re-habilitado en 16.3.4 con upstream fix). Suite **919/919** (87 archivos), `tsc --noEmit` EXIT 0, `lint` EXIT 0, `build` EXIT 0. | §16 | 0 |
| **E — Configuración de producción** | ✅ **COMPLETADA (2026-09-06).** `src/infrastructure/config/env.ts`: `RESEND_API_KEY`, `RESEND_FROM` y `APP_BASE_URL` pasan a **obligatorias en producción** vía `superRefine` cuando `NODE_ENV=production` (fail-fast con mensaje por variable; clave `NEXT_PHASE=phase-production-build` queda exenta para no romper el prerender de `next build` local — el runtime de producción no la define y sí exige, por eso el fallback a `localhost:3000` en `auth-email-deps.ts:25` se vuelve imposible en prod desplegado). `.env.example` reescrito y documentado (obligatorias en todos los entornos / obligatorias en producción / optativas). Tests nuevos: 15 en `env.test.ts` (producción exige las 3, dev/test/build no), suite **929/929**, tsc/lint/build EXIT 0. | §10 | D |
| **F — Auth hardening** | ✅ **COMPLETADA (2026-09-06).** **§12 — consumo atómico de tokens one-time:** port `AuthTokenStore.markUsed(userId, purpose)` → `consume(tokenId): Promise<boolean>` — `updateOne({_id: tokenId, used: false, expiresAt: {$gt: now}}, {$set: {used: true}})` y `modifiedCount === 1` como prueba de consumo; `resetPassword`/`verifyEmail` consumen por token específico ANTES de aplicar el cambio, el perdedor de la carrera recibe `INVALID_TOKEN_MESSAGE` (anti-replay bajo concurrencia). **§13 — `sessionVersion`:** campo en User (dominio + modelo Mongoose default 0 + both mappers; NO en `toJSON()`), `SessionClaims.sessionVersion?` y JWT (solo cuando está definido, legacy intacto); `getCurrentUser` SIEMPRE lee el user (1 `findById` indexado por request autenticado — costo deliberado para app financiera) y devuelve null si falta el user o `(payload.sessionVersion ?? 0) !== (user.sessionVersion ?? 0)` → el layout (main) redirige a /login. **Invalidación:** `resetPassword` (use case) y `changePasswordAction` incrementan `sessionVersion`; change además borra la cookie actual (`deleteSessionCookie`) forzando re-login; `verifyEmail`/`updateProfileAction` preservan la versión. login/register incluyen la versión en claims de la cookie. Tests: NUEVO `auth-token-repository.test.ts` — 5 tests con MongoMemoryServer real incl. **N=40 consumes concurrentes → exactamente 1 winner** (§25-C); `email-flows.test.ts` 12→15 (rechazo token usado, perdedor de carrera sin update, incremento de versión); `session.test.ts` 4→6 (round-trip versión + legacy sin key); `getCurrentUser.test.ts` reescrito 5→9 (match/mismatch, user ausente, legacy vs hardened); register/login outputs con `sessionVersion: 0`. Suite **992/992** (93 archivos), tsc/lint/build EXIT 0. | §12, §13 | E |
| **G — Protección `/api/monitor`** | ✅ **COMPLETADA (2026-09-07).** Guard anti-spike `MongoMonitorGuard` (§6): **cupo por IP** — `MAX_FINGERPRINTS_PER_IP=30` fingerprints ÚNICOS/IP/15 min (doc por ventana fija `monitor-fp:<ip>:<windowStartMs>` con array + `$addToSet` condicional atómico sobre `fingerprints.29`; fingerprint repetido pasa sin consumir cupo; TTL `expiresAt`; 40 concurrentes → exactamente 30 admitidos, testeado §25-C). **Cooldown global** — >30 fingerprints NUEVOS/5 min arma cooldown de 10 min (singleton `monitor:global`; `$inc` atómico + `$max` en `cooldownUntil`/`expiresAt` para que el TTL no borre el cooldown activo); la ruta rechaza con 429 `global_cooldown` ANTES de parsear. **Throttle de alertas** — `alertCooldown` en `MonitorDeps` (check-and-consume: 1ª llamada allowed, siguientes NO en la ventana): default `monitorAlertRateLimiter` (maxAttempts 1, 30 min) sobre `RateLimitModel` existente → máx. 1 email por fingerprint/30 min, sin modelo nuevo; fail-open si el throttle falla. Ruta `/api/monitor` ordena: rateLimit 120/15min → cooldown global → zod → `computeFingerprint` (igual que reportError) → `limitFingerprints` (429 `fingerprint_quota`) → `registerGlobalFingerprint` solo si `isNew` → `reportError`. Guard fail-open por contrato (nunca bloquea reportes legítimos por un fallo interno, nunca lanza, nunca 500). Suite **1007/1007** (94 archivos), tsc/lint/build EXIT 0. **Nuevas colecciones** `monitorfingerprints` y `monitorcooldowns` (unique + TTL vía autoIndex; sin migración manual — verificar en el cluster productivo al desplegar). | §6 | C |
| **H — Corrección de analytics** | ✅ **COMPLETADA (2026-09-07).** **§8 — activation con datos reales:** nuevo evento REGULAR `movementCreated` (hoy solo `firstMovement` dedup → activation ≥3 jamás alcanzable, 1 doc por workspace). **Ampliación pedida por el usuario (2026-09-07 "integra más eventos relevantes pero bien implementados"):** taxonomía completa de eventos de creación por módulo — `movementCreated`, `transferCreated`, `creditReceivedCreated`, `creditGrantedCreated`, `payableCreated` (5 nuevos REGULARES append; se suman a los existentes firstLogin/firstMovement dedup + register/accountCreated/dashboardViewed/saleCreated regulares). 5 server actions instrumentadas (movements, transfers, créditos recibidos/otorgados, payables) vía `trackAnalytics` best-effort (nunca lanza). Dashboard `/analytics` consulta los 5 eventos nuevos; activation = % de registered con ≥3 `movementCreated` dentro de 2 días (antes inalcanzable); `avgMovementsPerUser` = `totalMovementsCreated/totalRegistered` (antes ≈1 por el dedup). **§9 — página 100 % i18n:** server component `getT('Analytics')`, namespace `Analytics` nuevo con paridad es/en (español neutro), 11 cards de métricas + 4 derivadas sin strings hardcodeados. Tests: repositorio (5 eventos regulares → append paramétrico), movements (flujo exitoso emite firstMovement + movementCreated), transfers/credits-granted/payables (emisión verificada) + `credits/received/actions.test.ts` creado. Suite **1018/1018** (95 archivos), `tsc --noEmit` EXIT 0, `lint` EXIT 0, `build` EXIT 0. **Pendiente operativo:** al desplegar, activar `ANALYTICS_ENABLED=true` en producción (env ya documentado). | §8, §9 | 0 |
| **I — Dashboard: límites de carga** | ✅ **COMPLETADA (2026-09-07).** **23 archivos (4 nuevos).** Lectura por ventana real + índice compuesto, sin tocar los agregadores puros. **Ventana** (`src/core/application/dashboard/compute-dashboard-window.ts`, helper puro A2-compatible): `from = min(año civil actual, 6 meses civiles atrás)`, `to = inicio del próximo mes civil` (exclusivo, `$lt`) — cubre cards/mes actual, monthlyData 6 meses y yearlyData año civil; movimientos fuera de la ventana no aportan a ningún agregador (verificado contra `compute-dashboard-summary.ts`/`compute-yearly-evolution.ts`). **Repo:** `findByWorkspaceIdAndDateRange(ws, from, to)` (mismo sort `{date:-1, createdAt:-1}`, mismo orphan guard + `resolveBulkDependencies` que `findByWorkspaceId`) e **índice compuesto** `{workspaceId:1, date:-1, createdAt:-1}` (nombre explícito `workspace_date_createdAt`, en schema + scripts). **Balance (decisión Opción A aprobada por el usuario):** `findByWorkspaceIdForBalance(ws)` lee histórico completo con **proyección mínima** (sin `signedAmount`/`updatedAt`; `Movement` deriva `signedAmount` de type+amount en dominio) y mantiene el filtro live + `accountBalancesFromMovements` → **cifras idénticas R7-A, sin riesgo de orphans** (nada de `$group` replicando lógica financiera — respeta R14.6). `page.tsx`/`actions.ts` dividen lectura: ventana para el snapshot (con tz real en actions, default UTC en server render) + balance separado (Promise.all); selector de monedas `economicCurrencies` ahora solo ve monedas con datos en la ventana (mejora, no regresión). Scripts Fase G pattern: `scripts/ensure-dashboard-indexes.mjs` (dry-run/--apply idempotente, fail-closed si el índice existe con otras propiedades) + `scripts/verify-dashboard-indexes.mjs` (CONTRACT OK/VIOLATED). Tests: 9 del helper (bordes enero/diciembre/nov + TZ mes/año + invariantes) + 8 del repo (303→311) + 11 fakes actualizados. Suite **1041/1041** (96 archivos), `tsc --noEmit` EXIT 0, `lint` EXIT 0, `build` EXIT 0. **Pendiente operativo:** materializar `workspace_date_createdAt` en Atlas con `--apply` (autorización explícita requerida, igual que Fase G); en dev lo crea autoIndex al arrancar. | §7 | A (depende de la integridad de moneda para no esconder datos) |
| **J — CI/devops** | ✅ **COMPLETADA (2026-09-07).** `.github/workflows/ci.yml`: trigger de `branches: [main]` (rama inexistente — el repo usa `master`, el CI NUNCA corrió) → `[master]` (+ comentario de estrategia corregido); `npx playwright install` → `pnpm exec playwright install --with-deps chromium` (regla pnpm-only; `npx` queda fuera). **Fix de runtime al correr el CI por primera vez (2026-09-07):** jobs con `node-version: 20` rompían en el setup — pnpm 11.22.0 exige Node ≥ 22.13 (`node:sqlite` no existe en Node 20) → `node-version: 24` en quality y e2e (coherente con dev local). **Fix de typecheck limpio (CI, 2026-09-07):** `tsc --noEmit` fallaba con `Cannot find name 'LayoutProps'` en CI porque `.next/types` (LayoutProps/PageProps generados por Next) no existe hasta un build; localmente pasaba por builds previos → job quality gana `pnpm exec next typegen` ANTES del typecheck (genera tipos sin build completo). **Fix del job E2E (CI, 2026-09-07):** `pnpm test:e2e` fallaba con `Missing env file: .env.e2e` — el archivo está git-ignored; el job ahora declara `MONGODB_URI`(placeholder loopback local — `global-setup` arranca mongodb-memory-server en 37017 y el webServer `next start` hereda el env del job; un placeholder 27017 hacía ECONNREFUSED en cada request; Atlas guard aborta si la URI no resuelve a loopback)/`AUTH_SECRET`/`NEXT_LOCALE=en` y genera `.env.e2e` justo antes de correr la suite. **Fix del webServer E2E — deuda de la Fase E (CI, 2026-09-07):** `next start` corre con `NODE_ENV=production` y desde R14-E `env.ts` exige `RESEND_API_KEY`/`RESEND_FROM`/`APP_BASE_URL`; el `.env.e2e` nunca las tuvo → el webServer respondía `Invalid environment configuration` en cada request y TODOS los specs morían por timeout (el E2E quedó roto silenciosamente desde la Fase E; el CI con trigger en `main` nunca lo destapó). Fix: `.env.e2e` (local + job CI) gana placeholders de prueba — el E2E nunca envía emails reales (best-effort con log), así que valores dummy pasan la validación sin efectos. **Cierre del E2E (2026-09-07):** R14-B `MongoUnitOfWork.withTransaction` exige réplica → `global-setup` migra a `MongoMemoryReplSet` (count 1, wiredTiger, rs0, dbName en opts de replSet — TS2353 si va en instanceOpts); el binario default 8.2.6 crasheaba en Windows (0xC000001D en tcmalloc, exit 14, ~1 min tras arrancar, ghost reference que `stop()` no limpia) → pin `binary: 7.0.41`; flaky i18n toggle estabilizado con guard pre-hidratación (`expect().toPass()`). Suite E2E completa **24/24 verde local y en CI** (credits-pos transaccional, movements-transfers, dashboard-isolation, auth-accounts). Dominio de producción documentado: **`https://twincap.vercel.app`** (no `app.twincap.app`). `docs/beta-launch-plan.md` §8: branch protection requerida documentada (status checks `quality` + `e2e`, block force push/deletes; require PR/approvals NO aplican — fundador único mantenedor, push directo a master dispara Vercel). §9: smoke test post-deploy (P0.7) con checklist de 9 pasos (login real en incógnito, dashboard con datos, alta de movimiento, páginas legales, verify dashboard + monitor indexes → CONTRACT OK, logs `/api/monitor`, env analytics). Checklist §7 actual. | §17, P0.7 | D |
| **K — Arquitectura: violaciones puntuales** | ✅ **COMPLETADA (2026-09-07).** 4 violaciones §14/§15 corregidas SIN reestructurar capas ni cambiar comportamiento. **(§14a)** `register.ts` ya no importa infra: nuevo puerto `WorkspaceBootstrapper` (`ports.ts`, método `bootstrap(workspaceId)`), adaptador `MongoWorkspaceBootstrapper` (`infrastructure/seeding/user-bootstrap.ts`, envuelve `seedUser` sin duplicar lógica) inyectado como 8º parámetro posicional (convención del use case). **(§14b)** `logout.ts` ya no importa infra: nuevo puerto `SessionCookieManager` (`destroy()`; nombre NUEVO porque `SessionManager` ya existe como codec de token create/verify), adaptador `sessionCookieManager` en `session-cookie.ts`, inyectado por parámetro; la acción pasa el adaptador. **(§14c)** `build-dashboard-snapshot.ts` ya no importa componentes: NUEVO `core/application/dashboard/dashboard-types.ts` concentra los tipos puros (`DashboardSnapshot`, `DashboardAccountSnapshot`, `DashboardFilters`, `ScopeFilter`, `CurrencyBreakdown`, `SummaryTableRow`, `SerializedMovement`); los 5 archivos de componentes re-exportan desde core (import paths estables: page/actions/content/barrel intactos); el builder y su test importan desde core; tipos puros sin React. **(§15)** `findByIdRaw` encapsulado: contrato `repositories.ts:111` y `transfer-repository.ts:71` ahora exigen `(workspaceId, id)` con query `{ _id, workspaceId }` (regla workspaceId-first) — sin callers de producción (solo fakes de test, nullarios, siguen compilando). Tests: NUEVO `transfer-repository.test.ts` (3 tests, estilo mock-de-modelo local): query scoped, cross-workspace → null, no-match → null. Suite **1048/1048** (98 archivos), `tsc --noEmit` EXIT 0, `lint` EXIT 0, `build` EXIT 0. | §14, §15 | 0 |
| **L — Feedback persistente** | ✅ **COMPLETADA (2026-09-07).** **§11 resuelto.** Port out `FeedbackRecord`/`FeedbackStore` (`src/core/application/ports.ts`) + modelo `Feedback` (`src/infrastructure/models/feedback.ts`: kind/message/userId/email/locale/page + `result: 'delivered' | 'failed'` + `attemptedAt`; índices `userId` + compuesto `{userId, attemptedAt}`) + repo best-effort `MongoFeedbackRepository` (`src/infrastructure/repositories/feedback-repository.ts`: guard `isDbConnected` + stderr JSON `feedback_write_failed`, NUNCA lanza — un fallo de persistencia no rompe el submit). Dispatcher (`feedback-alerter.ts`) conserva el transporte Resend pero devuelve `FeedbackDispatchResult`: `{delivered:true}` / `{delivered:false, reason:'not_configured'|'transport_error'}` (fail-safe preservado, nunca lanza). `submitFeedbackAction` persiste el resultado REAL y solo reporta `feedbackSent` cuando el email salió; transport_error → `error.operationFailed` + registro `failed` (nunca "enviado" sin envío); dev sin provider → no-op silencioso SIN persistir (dev nunca spameado ni error falso); producción sin config → fallo honesto. Tests: 22 nuevos (modelo 6, repo 4, alerter 4, action 8 — incl. regresión §11: transport_error → error + registro `failed`, y dev gate no persiste). Suite **1070/1070** (102 archivos), tsc/lint/build EXIT 0. | §11 | 0 |
| **M — Cookie rename `gm_session` → `twincap_session`** | ✅ **COMPLETADA (2026-09-07).** `COOKIE_NAME` en `src/infrastructure/auth/session-cookie.ts:4` renombrada a `twincap_session` (mismo `COOKIE_OPTIONS`; set/get/delete usan la constante — 1 punto de cambio). Páginas legales actualizadas: `src/content/legal/es.ts` (tabla de cookies + texto "cookie de sesión") y `src/content/legal/en.ts` (idem) — sin tocar el resto del contenido. E2E `e2e/dashboard-isolation.spec.ts` actualizado (4 referencias): logout limpia la cookie nueva y aislamiento de tenants verifica valores distintos por contexto. **Test unitario NUEVO** `src/infrastructure/auth/session-cookie.test.ts` (4 tests): set/read/absent/delete usan `twincap_session` (cobertura local rápida del nombre). **Auditoría de residuos `gm_*`:** grep `gm_` en `src/` + e2e → SOLO la cookie (0 otros residuos en código; `globalmoney`/`"gm"` → 0 matches); docs de auditoría conservadas como evidencia (testimonial, no se tocan). **Efecto aceptado (pre-beta):** invalidación única de sesiones activas — el navegador conserva `gm_session` huérfana, la app lee `twincap_session` y trata a todos como log-out. Suite **1074/1074** (103 archivos), `tsc --noEmit` EXIT 0, `lint` EXIT 0, `build` EXIT 0, E2E **24/24** (incl. logout clears session cookie + tenant isolation). | §21 | F (misma zona de sesión) |
| **N — Mapper round-trip + capa de testing (§25)** | ✅ **COMPLETADA (2026-09-07).** Fix `src/infrastructure/mappers/movement.ts`: persistir/reconstruir `link.saleId` en ambas direcciones (`toMovementEntity` + `toMovementDocData`; antes solo `kind/refId/opId` — el campo I12 definido en dominio (R13-F4) + schema se perdía en el round-trip). **§25-D implementado:** NUEVO `src/infrastructure/mappers/movement-mapper.roundtrip.test.ts` — 4 tests con `MongoMemoryServer` + modelo Mongoose REAL (sin fakes): round-trip con `saleId` presente (igualdad semántica completa: id, workspaceId, accountId, categoryId, type, money, signedAmount, date, context, link, createdAt), sin `saleId` (standalone), sin link, y persistencia del `opId` (clave MOV-5). Cobertura §25 completa: A (Fase A), B (Fase B), C (Fases C/F/G), D (nuevo), E (mojón a futuro: staging real). **Fix de flakiness adjunto:** `password.test.ts` (bcrypt bajo carga paralela de la suite completa excedía el timeout default 5s → 20s explícito por test). Suite **1045/1045** (97 archivos), tsc/lint/build EXIT 0. | §24, §25 | A, C (bases para A/C) |
| **O — Backup/restore real + cierre de beta** | Backup + restore **REAL probado contra Atlas** (P0-c), no documental. Checklist de aislamiento (§19) ampliado y **ejecutado por el fundador**. Placeholders legales (§20) resueltos o decisión explícita para beta cerrada controlada. Smoke test post-deploy ejecutado. Actualizar `docs/definition-of-beta.md` (Grupo B) al cerrar P0-c y P0-b/smoke. | §18, §19, §20, P0.6, P0.7 | J, N |

## R14.4 — Orden de ejecución y dependencias

```
0 → D → E → (A, B, C) → F → G → (H, I) → J → N → (K, L, M) → O
```

- **0** primero (archivo histórico + maestro compacto; base de todo).
- **D** y **E** primeras entre las técnicas: upgrade rápido + configuración de producción; desbloquean CI (J) y email (F/O).
- **A/B/C** (moneda, atomicidad, rate limiter) independientes entre sí; B requiere la decisión de infraestructura de R14.6.
- **F** y **G** hardening (sesión + monitor) después del rate limiter real (C).
- **H/I** corrección de producto con la integridad de moneda ya cerrada (A).
- **N** después de A/C: valida la capa de persistencia con tests adversariales antes de tocar atomicidad.
- **K/L/M** limpieza estructural sin urgencia de orden interno.
- **O** cierra: requiere la mayoría de las anteriores para declarar la beta lista.

## R14.5 — Criterios de éxito

1. **0**: maestro compacto con estado + reglas R4.3 + plan R14 + protocolo; histórico testimonial con rondas 1–13 y bitácora; **0 contenido perdido** (todo encabezado del original existe en uno de los dos archivos).
2. **A**: ✅ nada con moneda ≠ moneda de la cuenta persiste ni se lee (movements, transfers, créditos, payables, ventas, abonos con cuenta de pago validada); catálogo almacena `currency` (backfill de items legacy ejecutado contra Atlas 2026-09-06 — 19 items/7 workspaces, verificados 0 sin currency); venta valida catálogo/cuenta/venta; créditos/payables con moneda inmutable; tests adversariales de moneda (§25-A) en verde — suite **958/958**, tsc/lint/build EXIT 0.
3. **B**: ✅ operaciones críticas multi-doc con atomicidad real (transacción sobre replica set): `createTransfer` y `createSale` envuelven toda su fase de escritura en `MongoUnitOfWork`; `TransactionHandle` opaco + port `UnitOfWork`; tests de fallo parcial (§25-B) con rollback total real contra `MongoMemoryReplSet` (0 documentos tras abort) + unit de rollback; suite **963/963**, tsc/lint/build EXIT 0.
4. **C**: ✅ IP real en rate limiter (`resolveClientIp` desde `headers()`: `x-real-ip` → primer `x-forwarded-for`, sanitizado; sin `_ip` del form — el frontend nunca lo enviaba); operación atómica (`findOneAndUpdate`+`$inc`+retry E11000) con índice único en `key`; 3 tests de concurrencia REAL (§25-C): 40 checks concurrentes → exactamente maxAttempts allowed, attempts 1..40 sin pérdidas; auth + profile + monitor cubiertos (monitor: gate 120/15min → 429; G añade fingerprint+cooldown). Suite **978/978**, tsc/lint/build EXIT 0. **Operativo Atlas ejecutado:** índice único `key_1` creado (0 duplicados en prod), TTL preservado.
5. **D**: ✅ `next` + `eslint-config-next` 16.3.4 instalados; suite **919/919**, `tsc --noEmit` EXIT 0, `lint` EXIT 0, `build` EXIT 0. CVEs de agosto 2026 cubiertos (GHSA-p293-qw3h-jr36 Windows RCE, GHSA-2xp9-vwfh-vxw4 AVIF RCE).
6. **E**: ✅ `env.ts` exige las 5 variables en producción (fail-fast con `superRefine` + `NODE_ENV`; `NEXT_PHASE` exento solo durante `next build`); `.env.example` documentado; fallback localhost imposible en prod desplegado.
7. **F**: ✅ un token solo se consume una vez bajo concurrencia (`consume(tokenId)` condicional `used:false`+`expiresAt>$now` con `modifiedCount===1` como prueba; N=40 concurrentes → exactamente 1 winner en test real MongoMemoryServer §25-C); password change/reset invalida sesiones previas (`sessionVersion` incrementa; `getCurrentUser` devuelve null en mismatch; change borra la cookie actual). Suite **992/992**, tsc/lint/build EXIT 0.
8. **G**: ✅ `/api/monitor` con rate limit + cooldown (cupo 30 fingerprints únicos/IP/15 min → 429 `fingerprint_quota`; cooldown global 10 min cuando >30 fingerprints nuevos en 5 min → 429 `global_cooldown`; gate por IP 120/15 min de R14-C conservado); throttle de alertas: máx. 1 email por fingerprint/30 min. Un abuso NO genera spam de alertas (testeado con concurrencia §25-C: 40 fingerprints → exactamente 30 admitidos, 10 rechazados, doc intacto). **POST-G:** throttle fail-CLOSED con timeout 1 s (Mongo caído NO spamea emails — el cooldown DB-backed no puede deduplicar, se suprime con log); carrera de `$addToSet` no-op corregida (`modifiedCount` distingue add real vs repeat concurrente → el contador global no se infla). **Índices = contrato de infraestructura (auditor 2026-09-07):** `scripts/ensure-monitor-indexes.mjs` materializó en Atlas (dry-run aprobado por el usuario + `--apply` autorizado) ambas colecciones + 4 índices (`key_1` unique + `expiresAt_1` TTL 0) ANTES de que el monitor reciba tráfico; verificador P0 `scripts/verify-monitor-indexes.mjs` → `CONTRACT OK` (2 colecciones + 4 índices reales, directo contra Atlas); checklist P0 en `docs/beta-launch-plan.md` §7 con la prueba idempotente (re-dry-run → 4× no-op); `autoIndex` queda solo como redundancia local/dev. Semántica temporal documentada en §7.1 (30/15, 30/5, 10, 1 email/30min — política, revisar con datos del beta). Suite **1024/1024**, tsc/lint/build EXIT 0.
9. **H**: ✅ activation con evento regular de movimientos (`movementCreated` ≥3 en 2 días — antes inalcanzable por dedup de `firstMovement`); eventos de creación por módulo (transfer/creditReceived/creditGranted/payable) consumidos por métricas reales del dashboard; página `/analytics` 100 % i18n es/en (paridad). Suite **1018/1018**, tsc/lint/build EXIT 0.
10. **I**: ✅ snapshot del dashboard lee por ventana con límite real (`computeDashboardWindow` pure + `findByWorkspaceIdAndDateRange` con índice compuesto `workspace_date_createdAt`); balance con histórico completo pero proyección mínima (`findByWorkspaceIdForBalance`, Opción A aprobada) → **cifras idénticas a la suite existente** (sin divergencia silenciosa, R7-A intacto); agregadores puros sin cambios. Suite **1041/1041** (96 archivos), tsc/lint/build EXIT 0. **Operativo Atlas pendiente:** índice compuesto por materializar con `ensure-dashboard-indexes.mjs --apply` (autorización explícita).
11. **J**: ✅ `ci.yml` dispara en push/PR a `master` (antes `main` — el CI no corría); `pnpm exec playwright install --with-deps chromium` (sin `npx`); branch protection documentada en `docs/beta-launch-plan.md` §8; smoke test post-deploy (P0.7) documentado y con checklist en §9 del mismo plan.
12. **K**: ✅ register/logout/build-dashboard-snapshot sin imports infra/component en core (puertos `WorkspaceBootstrapper`/`SessionCookieManager` + tipos de dashboard movidos a core con re-export desde componentes); `findByIdRaw` encapsulado y scoped por `workspaceId` (query `{_id, workspaceId}`). Suite **1048/1048**, tsc/lint/build EXIT 0.
13. **L**: ✅ feedback persiste con estado delivered/failed (`FeedbackRecord`/`FeedbackStore` + modelo `Feedback` + repo best-effort `MongoFeedbackRepository`; 22 tests nuevos: modelo 6, repo 4, alerter 4, action 8); el dispatcher distingue `delivered`/`not_configured`/`transport_error`; `submitFeedbackAction` reporta éxito SOLO con envío real y un fallo persiste `failed` (nunca "enviado" sin envío — regresión §11 testeada); dev sin provider → no-op silencioso sin persistir. Suite **1070/1070** (102 archivos), tsc/lint/build EXIT 0.
14. **M**: ✅ cookie `twincap_session` (único punto de cambio: `COOKIE_NAME` en `session-cookie.ts`; set/get/delete usan la constante); páginas legales es/en actualizadas; 0 residuos `gm_*` en código (grep verificado); nuevo test unitario de la constante (4 tests) + E2E actualizado (logout limpia cookie nueva, tenant isolation). Suite **1074/1074** (103 archivos), tsc/lint/build EXIT 0, E2E **24/24**. Efecto: invalidación única de sesiones activas (aceptado pre-beta).
15. **N**: ✅ **COMPLETADA (2026-09-07).** `movement.ts` persiste/reconstruye `saleId` (ambas direcciones); **§25-D ejecutado** — `movement-mapper.roundtrip.test.ts` con MongoMemoryServer + modelo real (4 tests: con saleId/igualdad semántica, sin saleId, sin link, opId persistido); cobertura §25 completa (A Fase A, B Fase B, C Fases C/F/G, D nueva, E mojón staging); fix flakiness bcrypt (`password.test.ts` timeout 20s). Suite **1045/1045** (97 archivos), tsc/lint/build EXIT 0.
16. **O**: backup+restore REAL probado contra Atlas; checklist de aislamiento ejecutado (Pass/Fail documentado); placeholders legales resueltos o decisión explícita; Definition of Beta sin gaps P0; smoke test ejecutado tras deploy.

## R14.6 — Restricciones

- **NO** features nuevas de producto (los cambios de analytics/dashboard son correcciones de exactitud, no features).
- **NO** Teams activo, **NO** Billing, **NO** Compras (reglas heredadas e inquebrantables).
- **NO** reescribir la arquitectura hexagonal (punto fuerte); K corrige violaciones puntuales creando puertos, no reestructurando capas.
- **NO** replicar la lógica de orphans/context/multi-moneda en agregaciones Mongo `$group` (R12-C1, riesgo de divergencia silenciosa de cifras).
- **NO** scripting/transformación de monedas inventada: toda transferencia cross-currency exige rate explícito (R3.14).
- Máximo **UNA dependencia nueva por fase**, documentada y justificada.
- Cada fase se implementa SOLO con aviso explícito (protocolo R3.13): auditoría → plan → aprobación → fase → tests → docs → detenerse.
- pnpm exclusivo (`pnpm exec playwright …`, nunca `npx`); commits convencionales sin Co-Authored-By; sin hook GGA; español neutro; i18n es/en paridad.
- La suite debe permanecer verde por fase: `pnpm test` + `tsc --noEmit`; `lint` y `build` EXIT 0.
- **Decisión Fase B (RESUELTA 2026-09-06):** **transacciones reales.** El plan R14.6 asumía erróneamente que el tier free M0 no soporta transacciones (lo catalogaba como standalone). **Verificado empíricamente contra el cluster productivo 2026-09-06** (sesión Mongoose + `startTransaction()` + aborted read → `TRANSACTION OK`): el M0 de Atlas es un **replica set** (3 nodos) y **soporta multi-document transactions SIN costo adicional**; su única limitación real es throughput (~100 ops/s). El inventario `docs/CONSISTENCY-AUDIT.md` reduce las ventanas crudas a **2 operaciones**: `createTransfer` (1 trasfer + 2 movimientos) y `createSale` (stock + sale + movements + credit). Decisión aprobada por el usuario: **implementar transacciones reales** en esas dos (sesiones Mongoose + `startTransaction`), con tests de fallo parcial §25-B que inyectan un error en el 2º documento y verifican rollback total (0 documentos corrompidos). Los deletes ya son idempotentes + tolerantes (cascada), no se tocan.

## R14.7 — Qué se hace y qué NO en esta ronda (resumen)

**SÍ:** integridad de moneda (A), atomicidad (B), rate limiter real (C), upgrade Next (D), configuración de producción (E), auth hardening (F), protección monitor (G), analytics correcta (H), dashboard limitado (I), CI correcta (J), puertos de arquitectura (K), feedback persistente (L), cookie rename (M), mapper round-trip + capa de testing §25 (N), backup/restore real + cierre de Definition of Beta (O).

**NO:** features nuevas; Teams/Billing/Compras; Mongoose 9; security headers avanzados; métricas sofisticadas/analytics completos; optimización avanzada (P2 de la auditoría); app móvil; factura electrónica; IA/BI.

---

## PROTOCOLO POST-COMPACTACIÓN

Si el contexto se compacta o inicia nueva sesión:

1. **Leer este archivo** (`docs/AUDIT-AND-PLAN.md`) y **`AGENTS.md`**.
2. **Buscar en Engram:** `mem_search(query: "TwinCap", project: "twincap")`.
3. **Identificar el estado actual** — Rondas 1–13 cerradas (resumen en la tabla ESTADO ACTUAL; detalle y bitácora testimoniales en `docs/AUDIT-AND-PLAN-HISTORY.md`). **Ronda 14 EN CURSO (2026-09-07):** Fase 0 completada (archivo histórico + maestro compacto + plan R14 aprobado); Fase D (Next 16.3.4), Fase E (configuración de producción), Fase A (integridad de moneda), Fase B (atomicidad), Fase C (rate limiter real), Fase F (auth hardening), Fase G (protección monitor), Fase H (corrección de analytics), Fase I (dashboard: límites de carga), Fase J (CI/devops), Fase N (mapper round-trip), Fase K (arquitectura: puertos `WorkspaceBootstrapper`/`SessionCookieManager`, tipos de dashboard movidos a core, `findByIdRaw` scoped por `workspaceId`; suite **1048/1048**) y **Fase L (feedback persistente — §11: port `FeedbackRecord`/`FeedbackStore` + modelo `Feedback` + repo best-effort; dispatcher honesto `delivered`/`not_configured`/`transport_error`; la acción persiste el resultado real y solo reporta envío cuando el email salió; suite **1070/1070** (102 archivos), tsc/lint/build EXIT 0)** y **Fase M (cookie rename `gm_session` → `twincap_session`; unique point of change en `session-cookie.ts`, legales es/en + E2E actualizados, 4 tests unitarios nuevos; suite **1074/1074** (103 archivos), tsc/lint/build EXIT 0, E2E **24/24**)** completadas. **G cerró post-fix con el contrato de índices MATERIALIZADO en Atlas** (2026-09-07): `scripts/ensure-monitor-indexes.mjs --apply` (dry-run aprobado + autorización explícita) creó `monitorfingerprints` + `monitorcooldowns` + 4 índices (`key_1` unique + `expiresAt_1` TTL 0) ANTES de que `/api/monitor` reciba tráfico; verificador `scripts/verify-monitor-indexes.mjs` → **`CONTRACT OK`** (2 colecciones + 4 índices reales, directo contra Atlas); re-dry-run idempotente → 4× no-op; `autoIndex` queda solo como redundancia local/dev; checklist P0 §7 de `docs/beta-launch-plan.md` marcado EJECUTADO. **I cerró con lectura por ventana del snapshot** (2026-09-07): `computeDashboardWindow` puro + `findByWorkspaceIdAndDateRange` con índice compuesto `workspace_date_createdAt` (schema + scripts ensure/verify Fase G pattern; **operativo Atlas EJECUTADO** por autorización explícita — `ensure-dashboard-indexes.mjs --apply` materializó el índice, verificador → `CONTRACT OK`, movements con 6 índices); balance conserva histórico completo con proyección mínima (`findByWorkspaceIdForBalance`, Opción A aprobada) → cifras R7-A idénticas; suite **1041/1041** (96 archivos), tsc/lint/build EXIT 0. **J cerró con CI sobre master** (2026-09-07): `ci.yml` trigger `[main]` (rama inexistente — CI nunca corría) → `[master]`; `npx` → `pnpm exec playwright install --with-deps chromium`; jobs `node-version: 20` → `24` (pnpm 11.22.0 exige Node ≥ 22.13 — el primer run del CI falló con `ERR_UNKNOWN_BUILTIN_MODULE: node:sqlite`); job quality gana `next typegen` antes del typecheck (`LayoutProps` vive en `.next/types`, ausente en CI limpio); job e2e genera `.env.e2e` (git-ignored) desde env del job antes de correr la suite y gana placeholders RESEND_*/APP_BASE_URL (deuda Fase E: `next start` en production exigía las 3 desde R14-E y el webServer moría en cada request) y `MONGODB_URI` apuntando al puerto fijo 37017 (el webServer hereda el env del job; un placeholder 27017 daba ECONNREFUSED en cada request); E2E cerrado con réplica (`MongoMemoryReplSet`) + binario mongod pin 7.0.41 (8.2.6 crasheaba en Windows) → suite **24/24 verde local y en CI**; dominio de producción `https://twincap.vercel.app`; branch protection documentada + smoke test post-deploy (P0.7) con checklist en `docs/beta-launch-plan.md` §8–§9. Orden de ejecución de fases en R14.4; criterios de éxito en R14.5.
4. **Esperar aviso del usuario para implementar la siguiente fase.**