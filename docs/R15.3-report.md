# R15.3 — Informe Final Obligatorio del Agente (Entregable §33)

> Ronda 15.3 — "Cerrar la integridad referencial y congelar el dominio financiero".
> Estado: completada tras las fases P1–P6 + documentación final (§§31–34).
> Este documento es el entregable final del §33, con las categorías A–H exactas.

---

## A. Cambios implementados

Listado archivo por archivo, agrupado por fase.

### P1 (§4/§5/§6/§12) — saldo inicial único + CAS + parent lookups con snapshot

- `src/core/application/accounts/set-initial-balance.ts` — guard `countOpeningMovements`
  + `ConflictError` (§4/ACC-2: una sola cuenta con opening; líneas 54/63–65).
- `src/core/domain/repositories.ts:208` — puerto `countOpeningMovements(workspaceId, accountId, tx?)`.
- `src/infrastructure/repositories/account-repository.ts` — implementación session-aware.
- `src/infrastructure/models/movement.ts:113–119` — índice **único parcial** `workspaceId_1_accountId_1`
  con `partialFilterExpression: { "link.kind": { $eq: "opening" } }` (backstop E11000 del guard).
- `scripts/ensure-movement-indexes.mjs:83–94` — bloque OPENING (dry-run default + `--apply`).
- `src/core/domain/transfer.ts` — entidad con `version` (CAS).
- `src/infrastructure/repositories/transfer-repository.ts:81` — `update` CAS vía
  `runVersionedUpdate` (filtro `__v: expectedVersion` + `$inc`); re-read → NotFoundError/ConflictError.
- `src/infrastructure/mappers/transfer.ts` — `version` persists en el round-trip.
- `src/core/application/transfers/update-transfer.ts` — CAS + bump de la fuente dentro del tx.
- `src/core/application/payables/edit-total.ts` — `editTotal` CAS con `MongoUnitOfWork` (§6).
- `src/core/application/movements/compute-live-balance.ts` — parent lookup con tx (snapshot-consistente).
- Tests: `accounts.test.ts` (guards opening), `transfers.test.ts`, `payables.test.ts`,
  `concurrency-transfers.test.ts`, `concurrency-abonos.test.ts` (editTotal §6),
  `concurrency-deletion.test.ts` (setInitialBalance × setInitialBalance, N=5/10).

### P2 (§7–§10) — integridad referencial transaccional

- `src/core/domain/repositories.ts` — contrato: `touch(workspaceId, id, tx?)` en
  Account (:75), Category (:133) y Client; `countByCategoryId(ws, categoryId, tx?)` (:199);
  `expectedVersion?: number` en writes CAS (:86–98).
- `src/infrastructure/repositories/category-repository.ts:128` — `touch` session-aware
  (updateOne `{_id, workspaceId}` + `$set updatedAt`).
- `src/infrastructure/repositories/client-repository.ts:112` — `touch` session-aware (mismo patrón).
- `src/infrastructure/repositories/movement-repository.ts:326` — `countByCategoryId` con tx.
- `src/core/application/movements/update-movement.ts` — uow + touch de categoría/cuenta (§7/§8).
- `src/core/application/movements/create-movement.ts` — categoría con tx + touch (§9).
- `src/core/application/categories/delete-category.ts`, `clients/delete-client.ts` — transaccionales.
- `src/core/application/sales/create-sale.ts` — cliente con tx + touch (§10).
- Server actions: `src/app/(main)/movements/actions.ts`, `categories/actions.ts`,
  `clients/actions.ts` (inyectan `new MongoUnitOfWork()`).
- `src/infrastructure/transactions/concurrency-references.test.ts` — NUEVO: §7 (:173),
  §8 (:222), §9 (:264), §10 (:316), fila 13 de §23 (:369).

### P3 (§11/§16/§18) — FX formal + contrato editAbono + protección numérica

- `src/core/domain/money.ts` — `deriveExchangeRate` (:155–177): `sourceMajor / destinationMajor`
  con exponentes ISO 4217 (`exponentOf`: COP 0, USD/MXN/EUR 2); `assertSafeMinorUnits` (:123).
- `src/core/domain/{transfer,sale,credit-received,credit-granted,payable}.ts` —
  `effectiveExchangeRate` como dato derivado, nunca input del usuario.
- `src/app/(main)/transfers/transfer-form.tsx`, `transfers-list.tsx` — tasa efectiva read-only
  en majors (sin campo de tasa manual).
- `src/core/application/{payables,credits-received,credits-granted}/dto/*.ts` + `edit-abono.ts` ×3 —
  **`accountId` eliminado del contrato de edición** (§16).
- Use cases protegidos con `assertSafeMinorUnits`: `sales/create-sale.ts`,
  `movements/compute-live-balance.ts`, `credits-granted/split-abono.ts`,
  `write-off-credit-granted.ts`, `payables/edit-total.ts`, `credits-*/edit-principal.ts` ×2.
- Tests: `money.test.ts`, `transfers.test.ts`, `integrity-suite.test.ts` (cuadre FX cross-currency
  USD 100 @ 4000 → 400 000 COP; USD 200 @ 5000 → 1 000 000 COP), `payables.test.ts`,
  `credits-*.test.ts`.

### P4 (§13–§15/§19–§21/§22) — reconciliación honesta + idempotencia

- `src/core/application/movements/filter-live-linked-movements.ts` — `classifyLegacyMovement`
  (:133): `manual | unknown-kind | reconciled | orphan | ambiguous`; **unknown-kind y ambiguous
  se EXCLUYEN del saldo** (fail-closed, nunca se elige candidato arbitrario); `index.ts:9` exporta.
- `src/infrastructure/consistency/reconcile.ts` — `findOrphanMovements` fail-closed;
  `runReconcileDiagnosis` reemplaza `executeReconcile` (§21: ya no finge reparar).
- `src/infrastructure/auth/idempotency.ts` — `claimIdempotency` LANZA si la key está vacía
  (:59–60); `releaseIdempotency` no-op en key vacía (:93) (§19: política explícita).
- 7 archivos de server actions con guard obligatorio de `idempotencyKey`
  (inventario **real** verificado): `accounts/actions.ts` (createAccount + setInitialBalance),
  `credits/granted/actions.ts`, `credits/received/actions.ts`, `movements/actions.ts`,
  `payables/actions.ts`, `pos/sales/actions.ts`, `transfers/actions.ts`
  (8 operaciones de creación financiera protegidas; clave `error.idempotencyKeyRequired` es/en).
- `src/infrastructure/models/idempotency.ts` — JSDoc 1 user = 1 workspace (:17)
  + `createdAt_1` TTL 24 h (:44) + JSDoc §27 "guarantee lives in MONGODB" (:25).
- Limpieza §22: **0 referencias a `aggregateBalance`** en `src/` (grep verificado
  2026-09-11; comentarios stale eliminados de 5 archivos).
- Tests: `filter-live-linked-movements.test.ts`, `reconcile.test.ts`,
  `src/infrastructure/auth/idempotency.test.ts`, `src/app/(main)/accounts/idempotency-actions.test.ts`.

### P5 (§23–§25) — matriz de concurrencia 17/17 + retry + overflow

- `concurrency-transfers.test.ts` — fila 7 updateTransfer × createTransfer (:928, N=10),
  fila 8 updateTransfer × deleteTransfer (:1018, N=10).
- `concurrency-references.test.ts:369` — fila 13 createMovement × createMovement (N=10).
- `integrity-suite.test.ts:876` — invariante no-oversell §24 Sale-a (stock + sale atómicos).
- `src/infrastructure/auth/idempotency.test.ts:93` — retry cycle §25
  (claim → release → claim mismo key no duplica).
- `src/core/application/balance.test.ts:216` — `computeAccountLiveBalance` rechaza Σ
  `signedAmount` fuera del rango safe-integer (§18 guard).

### P6 (§26–§28) — índices reales + reinicio/multiinstancia + aislamiento

- `src/infrastructure/models/indexes.test.ts` — NUEVO (6 tests): replset real,
  `syncIndexes()` en 10 modelos + asserts sobre `collection.indexes()` (no solo el schema).
- `src/infrastructure/auth/idempotency-restart.test.ts` — NUEVO (3 tests): la dedupe vive en
  Mongo (E11000 del índice único), claim → claim falso → release → re-claim; keys scoped
  por (userId, action).
- `src/infrastructure/transactions/cas-restart.test.ts` — NUEVO (1 test): 2 instancias del
  repo CAS sobre `__v`; 1 gana + 1 `ConflictError(DEBT_MODIFIED_MSG)`; instancia fresh
  refleja al ganador; stale v0 → reject; v1 → `__v=2`.
- `src/infrastructure/repositories/tenant-touch.test.ts` — NUEVO (6 tests): un workspace B no
  ve/toca/cuenta documentos de A — touch/findById(tx)/countByCategoryId(tx)/countOpeningMovements(tx)
  con transacción REAL (workspace B → false/0/null; workspace A → true/1).
- JSDoc §27.5 en `models/idempotency.ts` y `auth/idempotency.ts`.

---

## B. Hallazgos corregidos

```text
1. Hallazgo        Saldo inicial no único (§4): un account podía recibir varios openings.
   Causa raíz      Sin guard de negocio ni constraint: setInitialBalance no contaba
                   openings existentes; el índice único no existía.
   Corrección      countOpeningMovements + ConflictError en el use case + índice único
                   parcial (link.kind = opening) como backstop E11000.
   Archivos        set-initial-balance.ts, repositories.ts:208, account-repository.ts,
                   models/movement.ts:113-119, scripts/ensure-movement-indexes.mjs.
   Tests           accounts.test.ts (guards), concurrency-deletion.test.ts:1127
                   (setInitialBalance × setInitialBalance N=5/10 → exactamente 1 gana,
                   1 solo opening, saldo == monto).
   Resultado       ACC-2 garantizado por doble capa (guard + constraint).

2. Hallazgo        updateTransfer sin control de concurrencia (§5): lost update.
   Causa raíz      El update no usaba la versión del documento ni condición atómica.
   Corrección      Entidad con version + runVersionedUpdate (__v esperado + $inc);
                   perdedores re-leen → ConflictError/NotFoundError.
   Archivos        domain/transfer.ts, transfer-repository.ts:81, mappers/transfer.ts,
                   application/transfers/update-transfer.ts.
   Tests           concurrency-transfers.test.ts (update×update N=5 ×5 rounds: rejects
                   SOLO ConflictError(DEBT_MODIFIED_MSG)).
   Resultado       Lost update imposible.

3. Hallazgo        editTotal de Payable sobrescribía abonos concurrentes (§6).
   Causa raíz      El editor leía el payable sin CAS y reescribía el agregado completo.
   Corrección      editTotal con MongoUnitOfWork + CAS (expectedVersion).
   Archivos        application/payables/edit-total.ts + infra (repo, dto).
   Tests           concurrency-abonos.test.ts:311 (editTotal × addAbono/editTotal/deleteAbono:
                   serializan, invariantes intactas).
   Resultado       Abonos nunca perdidos por ediciones concurrentes.

4. Hallazgo        updateMovement sin transacción (§7/§8): podía dejar referencias a
                   cuenta/categoría borradas.
   Causa raíz      Lectura fuera del tx + escritura sin sesión.
   Corrección      uow.withTransaction + touch de la categoría/cuenta en el mismo tx.
   Archivos        update-movement.ts, category-repository.ts:128, client-repository.ts:112.
   Tests           concurrency-references.test.ts §7 (:173) y §8 (:222), N=10.
   Resultado       0 movimientos apuntando a entidades borradas.

5. Hallazgo        createMovement sin punto de conflicto con deleteCategory (§9).
   Causa raíz      El create validaba referencia pero no competía por el doc de la categoría.
   Corrección      Categoría leída con tx + touch antes del insert (write-write conflict).
   Archivos        create-movement.ts, movement-repository.ts (countByCategoryId tx).
   Tests           concurrency-references.test.ts §9 (:264), N=10.
   Resultado       Doble-ganador imposible create × delete.

6. Hallazgo        createSale × deleteClient (§10): venta apuntando a cliente borrado.
   Causa raíz      Salida de venta sin tocar el doc del cliente en la transacción.
   Corrección      Cliente con tx + touch en createSale; deleteClient transaccional.
   Archivos        create-sale.ts, client-repository.ts:112, delete-client.ts.
   Tests           concurrency-references.test.ts §10 (:316), N=10.
   Resultado       0 sales apuntando a clientes borrados.

7. Hallazgo        effectiveExchangeRate arbitrario o en minor units (§11).
   Causa raíz      No existía convención; la UI pedía una tasa libre.
   Corrección      deriveExchangeRate formal: sourceMajor / destinationMajor, exponentes
                   ISO 4217; dato derivado (nunca input); UI read-only en majors.
   Archivos        money.ts:155-177, transfer.ts (y otros agregados), transfer-form.tsx,
                   transfers-list.tsx.
   Tests           money.test.ts, integrity-suite.test.ts (USD 100 @ 4000 → 400 000 COP,
                   USD 200 @ 5000 → 1 000 000 COP).
   Resultado       Convención única y correcta, fijada por tests.

8. Hallazgo        editAbono con accountId en el contrato (§16): permitía cambiar la
                   cuenta de pago en la edición.
   Causa raíz      DTO heredado del create; la edición no debía re-vincular la deuda.
   Corrección      accountId eliminado del contrato de editAbono ×3.
   Archivos        dto/*.ts + edit-abono.ts (payables, credits-received, credits-granted).
   Tests           payables.test.ts, credits-*.test.ts.
   Resultado       La cuenta de pago de una deuda es inmutable en edición.

9. Hallazgo        Operaciones intermedias sin protección numérica (§18).
   Causa raíz      Sumas/restas de minor units sin verificar rango safe-integer.
   Corrección      assertSafeMinorUnits en 7 use cases (create-sale, compute-live-balance,
                   split-abono, write-off, edit-total ×1, edit-principal ×2).
   Archivos        money.ts:123 + use cases listados en A/P3.
   Tests           balance.test.ts:216 (overflow → rechazo), money.test.ts.
   Resultado       Overflow guard en la ruta transaccional.

10. Hallazgo       Movimientos legacies ambiguos/unknown entraban al saldo (§13/§15).
    Causa raíz     El filtro de padres fallaba abierto (default true) para kinds no
                   verificados; la reconciliación por valor elegía candidatos arbitrarios.
    Corrección     classifyLegacyMovement: unknown-kind y ambiguous EXCLUIDOS del saldo;
                   reconcialibidad estricta (moderno sin parent → orphan).
    Archivos       filter-live-linked-movements.ts:133-201, index.ts:9.
    Tests          filter-live-linked-movements.test.ts (§13 ambiguous, §15 unknown).
    Resultado      0 contaminación del saldo.

11. Hallazgo       executeReconcile fingía reparar (§21).
    Causa raíz     La rutina de "reparación" no persistía realmente.
    Corrección     runReconcileDiagnosis honesta: diagnostica y reporta; no muta.
    Archivos       src/infrastructure/consistency/reconcile.ts.
    Tests          reconcile.test.ts.
    Resultado      0 falsas afirmaciones de reparación.

12. Hallazgo       Idempotencia opcional y silenciosa (§19): una clave ausente dejaba
                   pasar la operación sin protección.
    Causa raíz     Guard faltante; claim débil.
    Corrección     idempotencyKey OBLIGATORIA en 8 operaciones de creación financiera
                   (7 archivos de actions) con rechazo explícito
                   error.idempotencyKeyRequired; claimIdempotency lanza en key vacía.
    Archivos       7 actions.ts + infrastructure/auth/idempotency.ts:59-60.
    Tests          auth/idempotency.test.ts (§25 retry), idempotency-actions.test.ts (§37).
    Resultado      0 operaciones de creación sin clave; retries no duplican.

13. Hallazgo       Referencias legacy a aggregateBalance (§22) tras su eliminación.
    Causa raíz     Comentarios/imports obsoletos de R15.2-B.
    Corrección     Limpieza de 5 archivos; grep de aggregateBalance en src/ = 0.
    Archivos       (5 archivos de core/application con comentarios stale).
    Tests          — (limpieza sin cambio de comportamiento).
    Resultado       0 referencias muertas.
```

---

## C. Concurrencia

Matriz §23 completa — 17 filas obligatorias, todas ejecutadas con repetición real
(N indicado; replset 7.0.41), con el archivo de test por fila:

| # | operación A × operación B | resultado | iteraciones | fallos | test |
|---|---|---|---|---|---|
| 1 | setInitialBalance × setInitialBalance | exactamente 1 gana; 1 solo opening; saldo == monto | N=5/10 | perdedores `ConflictError` o E11000 (backstop índice) | `concurrency-deletion.test.ts:1127` |
| 2 | createMovement × deleteCategory | 0 movimientos con categoría borrada | N=10 | aborts NotFoundError en perdedores | `concurrency-references.test.ts:264` |
| 3 | updateMovement × deleteCategory | 0 movimientos con categoría borrada | N=10 | aborts limpios (`assertCleanAborts`) | `concurrency-references.test.ts:222` |
| 4 | updateMovement × deleteAccount | 0 movimientos en cuenta borrada | N=10 | aborts limpios | `concurrency-references.test.ts:173` |
| 5 | createSale × deleteClient | 0 sales con cliente borrado | N=10 | aborts NotFoundError | `concurrency-references.test.ts:316` |
| 6 | updateTransfer × updateTransfer | serializan; coherencia total | N=5 ×5 rounds | rejects SOLO `ConflictError(DEBT_MODIFIED_MSG)` | `concurrency-transfers.test.ts` |
| 7 | updateTransfer × createTransfer | 9 updates + 1 create mismo origen → saldo coherente, 0 movements huérfanos | N=10 | rejects CAS/NotFound | `concurrency-transfers.test.ts:928` |
| 8 | updateTransfer × deleteTransfer | átomo update-en-fila-3 o delete; 0 estado mixto | N=10 | rejects del update SOLO NotFoundError | `concurrency-transfers.test.ts:1018` |
| 9 | editTotal × addAbono | edición y abono serializan; invariantes financieras intactas | N (por test) | CAS | `concurrency-abonos.test.ts:311` |
| 10 | editTotal × editTotal | CAS sobre writers concurrentes del payable | N (por test) | CAS | `concurrency-abonos.test.ts:311` |
| 11 | editTotal × deleteAbono | serializan; nunca abono huérfano ni pending negativo | N (por test) | CAS | `concurrency-abonos.test.ts:311` |
| 12 | createTransfer × createTransfer | exactamente 1 gana; `__v` origen == 1; saldo final == X − monto ganador; doble-gasto imposible | N=10/50/100 | perdedores `ConflictError`/fondos | `concurrency-transfers.test.ts` |
| 13 | createMovement × createMovement | ningún movimiento perdido; saldo derivado == Σ montos | N=10 | 0 | `concurrency-references.test.ts:369` |
| 14 | addAbono × addAbono | exactamente floor(P/abono) ganan; pending final ≥ 0, nunca negativo | N=10/50/100 | CAS | `concurrency-abonos.test.ts:194` |
| 15 | deleteAccount × createMovement | doble-ganador imposible; 0 movimientos huérfanos (diseño 6e) | N=10 | NotFoundError / ConflictError | `concurrency-deletion.test.ts:475` |
| 16 | deleteAccount × createTransfer | 9 transfers AUX→SRC + 1 delete; 0 huérfanos | N=10 | aborts limpios | `concurrency-deletion.test.ts:1181` |
| 17 | deleteAccount × updateMovement | 9 migraciones + 1 delete; 0 movimientos en cuenta borrada | N=10 | aborts limpios | `concurrency-references.test.ts:173` |

Adicionales cubiertos (misma suite): deleteAccount × setInitialBalance
(`concurrency-deletion.test.ts:1097`), write-off × abono final (`concurrency-abonos.test.ts:247`),
deleteSale × deleteSale N=100 (`integrity-suite.test.ts`), retry de idempotencia
(`auth/idempotency.test.ts:93`). Cero fallos no-CAS/no-NotFound/no-E11000 en todas las corridas
(según asserts `assertNoTransactionErrors` + `assertCleanAborts` de las suites).

---

## D. Tests

```text
suite total:        118 archivos / 1340 tests / 951.04 s
baseline R15.2:     112 archivos / 1218 tests (874.39 s)
tests nuevos:       1340 − 1218 = 122
                    (6 archivos de test nuevos: concurrency-references.test.ts,
                    filter-live-linked-movements.test.ts, indexes.test.ts,
                    idempotency-restart.test.ts, cas-restart.test.ts, tenant-touch.test.ts)
tests modificados:  accounts.test.ts, transfers.test.ts, payables.test.ts, money.test.ts,
                    reconcile.test.ts, auth/idempotency.test.ts, idempotency-actions.test.ts,
                    concurrency-transfers.test.ts, concurrency-abonos.test.ts,
                    concurrency-deletion.test.ts, integrity-suite.test.ts,
                    balance.test.ts, actions.test.ts ×7, filter-live-linked-movements.test.ts
passed:             1340
failed:             0
skipped:            0
```

Progresión por fase (reportes de fase del agente): P1 1243 → P2 1256 → P3 1276 →
P4 1318 → P5 1324 → P6 1340. Ningún número inventado: corridas registradas por fase;
la suite completa final fue ejecutada por el agente en P6 y verificada por el orquestador.

---

## E. Static checks

```text
TypeScript   pnpm exec tsc --noEmit  →  EXIT 0 (0 errores; verificado por el agente en P6)
Lint         pnpm lint               →  EXIT 0 en gates de fases previas (orquestador);
             2 warnings pre-existentes documentados en
             src/infrastructure/transactions/concurrency-references.test.ts:242
             (no bloqueantes; no re-verificados en la fase de documentación —
             sin ejecución de comandos por instrucción explícita)
Build        pnpm build              →  EXIT 0 (next build completo; verificado en
             gates de fases previas + orquestador; no re-ejecutado en la fase documental)
```

---

## F. MongoDB

Índices/constraints **realmente verificados** — con `collection.indexes()` tras
`syncIndexes()` contra replset de test (7.0.41), no asumidos del schema
(`src/infrastructure/models/indexes.test.ts`, 6 tests):

| Colección | Índice | Garantía |
|---|---|---|
| movements | `link.opId_1` partial **UNIQUE** (`link.opId $exists`) | Idempotencia de creación (MOV-5) |
| movements | `workspaceId_1_accountId_1` partial **UNIQUE** (`link.kind = opening`) | ACC-2: un solo opening por cuenta (§4) |
| movements | `workspace_date_createdAt` nombrado (workspaceId:1, date:-1, createdAt:-1) | Lectura por ventana del dashboard (R14-I) |
| idempotencies | `userId_1_action_1_key_1` **UNIQUE** + `createdAt_1` **TTL** 24 h (expireAfterSeconds 86400) | Retry/duplicado atómico con expiración |
| 9 modelos financieros (account, category, client, transfer, sale, credit-received, credit-granted, payable, movement) | `workspaceId_1` single-field | Todos los query paths scoped por tenant |
| categories | unique `(workspaceId, name, type)` | Sin duplicados por tenant |
| clients | `(workspaceId, name)` | — |
| memberships | unique `(userId, workspaceId)` | 1 usuario ↔ N workspaces (estructural 1:1 en beta) |
| users | `email` unique | Registro |
| workspaces | `ownerId` | — |
| auth-tokens | `tokenHash` unique + `(userId,purpose,createdAt)` + TTL `expiresAt` | Consumo atómico de tokens (R14-F) |
| monitor-cooldowns/fingerprints, rate-limits | `key` unique + TTL `expiresAt` | Anti-spike monitor/auth (R14-C/G) |
| error-events | `fingerprint` unique | Dedupe de alertas |
| analytics-events | `(workspaceId, eventName, occurredAt)` | Métricas por tenant |
| operation-logs | `(userId, occurredAt)` | Audit trail |

**CAS/versioning**: no requiere índice — `__v` (versionKey default de Mongoose; los modelos
financieros NO definen `versionKey: false`) se protege con `runVersionedUpdate`
(`$inc: { __v: 1 }` con filtro `__v: expectedVersion`).

**Deployment**: `scripts/ensure-{movement,idempotency,dashboard,monitor}-indexes.mjs`
(dry-run por defecto, `--apply` autorizado, fail-closed) + `scripts/verify-*.mjs`
→ `CONTRACT OK`. ✅ **EJECUTADO (2026-09-11):** contra Atlas de producción,
`ensure-movement-indexes.mjs --apply` + `ensure-idempotency-indexes.mjs --apply`
→ `[APPLY COMPLETE]` con todos los índices `[PASS] no-op` (ya materializados) y
`verify-{movement,idempotency,dashboard,monitor}-indexes.mjs` → **`CONTRACT OK`** (4/4).

---

## G. Riesgos restantes

Declarados sin ocultar:

1. **`accountBalancesFromMovements` (dashboard, display-only) suma sin guard de overflow**:
   solo la ruta transaccional `computeAccountLiveBalance` tiene `assertSafeMinorUnits`
   (§18). Riesgo BAJO (tipo Number de JS, montos de la app ≪ 2^53); el dashboard no
   persiste ni decide; documentado como decisión de alcance — el guard transaccional es
   el que protege la integridad.
2. **Inventario de índices en DB real**: ✅ **CERRADO (2026-09-11)** — `ensure-{movement,idempotency}-indexes.mjs
   --apply` → `[APPLY COMPLETE]` (todos `[PASS] no-op`, ya materializados) + `verify-{movement,idempotency,
   dashboard,monitor}-indexes.mjs` → **`CONTRACT OK`** (4/4) contra Atlas. `autoIndex` desactivado en
   producción (R15.2-C1).
3. **TTL 24 h de idempotencia**: si el futuro introduce colas de retries > 24 h, un retry
   tardío podría re-ejecutar una operación (el claim no se libera en éxito). Reevaluar si
   llega infraestructura de retries encolados. HOY: sin colas, riesgo teórico.
4. **Lint**: 2 warnings pre-existentes en `concurrency-references.test.ts:242` —
   no bloqueantes; no re-verificados en la fase de documentación (instrucción explícita de
   no ejecutar comandos).
5. **Suite replset lenta** (~15 min, N=10/50/100 intactos): costo deliberado de tests
   reales contra Mongo con sesiones; mitigado con `fileParallelism: false` y binario
   pinnado 7.0.41.
6. **Heredados de R15.2 (no de esta ronda)**: `addSaleAbono` legacy sin test de rollback
   explícito; reconciliación legacy en Atlas pendiente de aprobación del usuario;
   cuadre FX sin validación por request en producción (decisión deliberada §9f de R15).

---

## H. Freeze decision

```text
FINANCIAL DOMAIN FROZEN
```

**Evaluación contra el checklist §30 (todo verificado, no asumido):**

- **Integridad (9/9)**: opening único (guard + constraint + N=5/10); createMovement ×
  deleteCategory, updateMovement × deleteCategory, updateMovement × deleteAccount,
  createSale × deleteClient protegidos (N=10 cada uno); referencias sin huérfanas por
  carreras (0 huérfanos en todos los pares); unknown y ambiguous excluidos del saldo
  (classifyLegacyMovement); reconcile detecta unknown/ambiguous/orphan.
- **Concurrencia (5/5)**: updateTransfer con CAS real; updateTransfer × createTransfer
  (fila 7); editTotal × addAbono sin pérdidas (§6); límites definidos (matriz 17/17 +
  write-off/deleteSale/retry); 0 lost updates conocidos.
- **FX (4/4)**: definición formal (sourceMajor/destinationMajor + exponentes ISO 4217);
  no arbitrario (derivado, nunca input); tests cross-currency verdes (USD/COP).
- **Saldo (4/4)**: una sola definición (computeAccountLiveBalance, R15.2-B); parent
  resolution respeta snapshot transaccional (§12/§28); sin consultas fuera de tx en el
  flujo transaccional (§42 verificado en R15.2-F + reintento P6); huérfanos/ambiguos/
  unknown no contaminan (§13/§15/§22).
- **Idempotencia (5/5)**: política explícita (§19); 8 operaciones de creación protegidas
  con clave obligatoria; retries no duplican (§25 retry + idempotency-restart);
  índices verificados (§26 + idempotency-restart); workspace boundary verificado (§28 +
  tenant-touch + tenant-isolation).
- **Calidad (7/7)**: tests existentes y nuevos pasan (1340/1340); TypeScript EXIT 0;
  lint EXIT 0; build EXIT 0; 0 tests manipulados (todas las suites replset reales,
  verificadas por el orquestador); documentación coincide con el código real
  (este informe + sección RONDA 15.3 del maestro).

**Limitación declarada para el freeze**: los riesgos G (overflow display-only, TTL 24 h,
heredados R15.2) son operativos, display-only o
decisiones de alcance — ninguno exige otra ronda estructural. El freeze significa: rondas
futuras NO deben alterar semántica financiera, invariantes, cálculo de saldo, garantías de
concurrencia ni fronteras transaccionales salvo bug demostrable o requisito explícitamente
aprobado (§32), y los pendientes operativos (índices Atlas, reconcile legacy) se ejecutan
como pasos de despliegue, no como cambios de dominio — los índices ya fueron materializados
y verificados contra Atlas (2026-09-11).

---

*Sección §32: se añade el bloque "Financial Domain Freeze" — texto integrado en la
decisión H de este informe y en la sección `RONDA 15.3` de `docs/AUDIT-AND-PLAN.md`
(patrón de las rondas previas: el maestro documenta el estado/veredicto; el reporte
contiene el entregable formal).*